node_modules
dist
storybook-static
.DS_Store
*.log
.env
.env.local
*.tsbuildinfo
vite.config.ts.timestamp-*.mjs

# Personal Claude Code settings (keep launch.json shared, ignore local overrides)
.claude/settings.local.json
.agents/
.claude/skills/
skills-lock.json

# Engine-resolved token snapshots — generated from a live Storybook subtree,
# dated, and stale the moment tokens.scss changes. Useful while measuring
# contrast; not a source of truth, so not carried in the repo.
docs/token-dump-*.json
