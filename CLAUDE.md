# @ui/lib — agent context

This file is auto-loaded into every Claude Code session opened inside this repo. Read it before writing any code that lives under `src/`. Deviations from these rules should be surfaced back to the user before they land — don't silently reshape the system.

> **Merging an older copy of this library in?** Read
> [`REBUILD-GUIDE.md`](./REBUILD-GUIDE.md) first — it is written for exactly that, and
> it assumes you have read this file.

---

## What this repo is

A React + SCSS component library. 60 shipped component families, one shared token file, zero third-party UI libraries. Every visual value comes from `src/styles/tokens.scss`. Every class name follows BEM under a `ui-` prefix.

## Hard rules (never break without asking)

1. **Runtime deps are `lucide-react` only — with ONE scoped exception.** `react` and `react-dom` are peer-deps. If you feel like reaching for `class-variance-authority`, `clsx`, `@radix-ui/*`, `framer-motion`, `motion`, `tailwindcss`, `@emotion/*`, `styled-components`, `@floating-ui/*` — stop and ask. The user has explicitly rejected each of these.
   **The exception (owner-approved 2026-08-09):** `react-markdown`, `remark-gfm` and `highlight.js` may be imported **only under `src/components/ChatMarkdown/`**, which ships on the **`@ui/lib/markdown` subpath** and is deliberately NOT exported from `src/index.ts` — the main entry stays dependency-free, and the build gate (`grep react-markdown dist/index.js` must be empty) enforces it. Do not widen this exception to any other component, and never add `rehype-raw` (it would reopen the XSS surface ChatMarkdown's design closes).
2. **No Tailwind, no CSS-in-JS.** Styling is SCSS in `.scss` files, imported by the component's `.tsx` as a side effect (`import './Component.scss'`).
3. **Every value comes from a token.** Grep `src/styles/tokens.scss` before writing any hex, rgba, or px literal. If a value isn't tokenized, add a token in `tokens.scss` first, then use it. See "Rare exceptions" below.
4. **Every component gets `forwardRef` + `displayName` + `className` passthrough.** Full stop.
5. **Class names use BEM under a `ui-` prefix** — `.ui-button`, `.ui-button__label`, `.ui-button--error`, etc. Never CSS Modules. Never Tailwind-in-JSX.
6. **`id` is a required prop** on every non-trivial component (Button, Input, Dialog, etc.). It seeds child IDs (`${id}-spinner`, `${id}-title`, `${id}-error`) so aria-relationships wire up cleanly.
7. **Storybook is where you verify.** Every new component needs stories. The user runs `npm run storybook` on port 6006. Screenshots are the primary regression test.

### Rare exceptions to rule 3

The following unavoidable literals live in the codebase intentionally — don't rewrite them:
- `-3px` on Tooltip arrow position (component-internal geometry, not a design-system value)
- `1000px` in Input's AND InputGroup's `-webkit-autofill` box-shadow (autofill kill-switch)
- `12px/16px/20px` Switch thumb travel distances (component-internal geometry per size)
- `380px/300px` Toast stack width / card min-width, `480px/560px` Command max-height / dialog width, `140px/200px` TabBar tab min/max width (panel geometry, deliberately not design-system values)
- `outline-offset: -1px` throughout Button (idiomatic inset outline)
- `text-underline-offset: 3px` in Breadcrumb links
- `width/height: 1px` sr-only patterns (Attachment, Breadcrumb)
- Tokens themselves in `tokens.scss` (the primitives literals)

Audited 2026-07: everything else in component SCSS is tokenized — zero hex/rgba/cubic-bezier literals, all durations/easings/border-widths/focus-rings via `var(--…)`.

---

## Non-negotiable file structure per component

```
src/components/{Name}/
├── {Name}.tsx            # forwardRef, displayName, prop destructure, ...rest spread
├── {Name}.types.ts       # Props + variant/size union types
├── {Name}.scss           # BEM global classes, plain .scss (never .module.scss)
├── {Name}.stories.tsx    # Playground + AllStates + variant/size stories
├── {Name}.constants.ts   # ONLY if the component has runtime constants (rare)
└── index.ts              # default export + type re-exports
```

Then wire it into `src/index.ts` as a named export.

## Component API conventions (do these on EVERY component)

```tsx
import { forwardRef } from 'react';
import type { WidgetProps } from './Widget.types';
import './Widget.scss';

const Widget = forwardRef<HTMLButtonElement, WidgetProps>(
  (
    { id, label, variant = 'default', size = 'default', className, ...rest },
    ref,
  ) => {
    return (
      <button
        {...rest}                          // spread first so our controlled props win
        ref={ref}
        id={id}
        className={`ui-widget ui-widget--${variant} ui-widget--sz-${size}${className ? ' ' + className : ''}`}
        type="button"
      >
        {label}
      </button>
    );
  },
);

Widget.displayName = 'Widget';

export default Widget;
```

Types file:
```ts
export type WidgetVariant = 'default' | 'brand' | 'error';
export type WidgetSize = 'sm' | 'default' | 'lg';

export type WidgetProps = Omit<
  React.ButtonHTMLAttributes<HTMLButtonElement>,
  'onClick'         // if we constrain onClick's signature
> & {
  id: string;
  label?: string;
  variant?: WidgetVariant;
  size?: WidgetSize;
  className?: string;
};
```

Key points:
- **Extend `React.*HTMLAttributes`** for the underlying element so all HTML props flow through via `...rest`
- **`Omit`** only the specific keys we want to redefine (usually `style`, `size`, `onClick`) — leave everything else intact
- **Spread `{...rest}` FIRST** in JSX, then set our controlled props (`id`, `className`, `type`, `disabled`, `onClick`) after so consumer overrides can't clobber our internals

---

## Shared utilities

Reuse these — don't reinvent.

- **`src/hooks/useMounted.ts`** — returns `true` after client-side mount. Use in every portal-rendering component (Dialog, Tooltip, DropdownMenu already do). Prevents SSR mismatches.
- **`src/hooks/useIsMobile.ts`** — SSR-safe `matchMedia` breakpoint hook (default 768px). Used by Sidebar to swap to a Drawer on small viewports; reuse for any responsive branch.
- **`src/hooks/useAutosizeTextarea.ts`** — `useAutosizeTextarea(value, { maxRows, minRows })`; grows a `<textarea>` to fit its content up to `maxRows`, then scrolls. Recomputes on every value change via an isomorphic layout effect (SSR-safe). Powers `ChatComposerInput`; returns a ref to attach to the textarea.
- **`src/hooks/useStickToBottom.ts`** — `useStickToBottom(deps, { threshold })`; keeps a scroll container pinned to the newest content unless the user scrolled up. Returns `{ ref, isPinned, scrollToBottom }` for a jump-to-latest affordance. Powers `ChatMessageList` (which feeds it a ResizeObserver tick so the follow fires on any growth, incl. streaming text). **The follow-to-bottom effect is intentionally ordered *before* the scroll listener** so it pins on mount before the listener's first measurement can mark it detached — don't reorder.
- **`src/hooks/useStreamingText.ts`** — `useStreamingText(target, { charsPerTick, intervalMs, enabled })`; reveals `target` progressively (the "typing out" effect) and returns `{ text, isStreaming, isDone }`. Presentational + dep-free: pass the full string to watch it type, or append tokens to `target` and the reveal catches up; a non-forward-extension of `target` restarts it. Pairs with `ChatBubble`'s `streaming` caret. One continuous ticker (reads `target` via ref, so it isn't torn down per character).
- **`src/hooks/usePointerTilt.ts`** — `usePointerTilt(enabled)`; returns a ref. Writes **three CSS custom properties** and nothing else: `--mx`/`--my` (−1…1, origin at the element's centre) and `--on` (0 at rest, 1 while tracking). All the movement is a `calc()` off those three in CSS, so React never sees the pointer — routing it through `useState` would re-render the subtree at pointer rate. Measures the rect on `pointerenter` (not per move — `getBoundingClientRect()` forces layout), batches writes into one rAF, and **never attaches under `prefers-reduced-motion`**, so the CSS falls back to its rest values with nothing to undo. Powers `Mark`'s `motion="tilt"`; promoted out of the deeper-theming prototype 2026-08-08. **Note it flushes through rAF — a backgrounded tab never fires one, so a test that awaits a frame there hangs.**
- **`src/hooks/useRipple.ts`** — opt-in Material ripple, **hook only** — no component turns it on for you (**Button deliberately has no `ripple` prop**). Returns `{ onPointerDown }` to spread on any `.ui-ripple` element; imports `ripple.scss` as a side effect. Wave expands over `--duration-ripple` (600ms, `--ease-out`); removed on `animationend` **plus a 900ms `setTimeout` fallback** (animationend doesn't fire under reduced motion / backgrounded tab). Use it only where the ripple is seen and helps — touch-first in-place actions (toggle, add-to-cart, stepper); **not** on navigation, desktop/mouse-only UIs, or anything that closes on click. Full when/how guidance lives in the hook's JSDoc and the `Hooks/useRipple` stories.
- **`src/utils/computePosition.ts`** — positions a floating element relative to a trigger. `Side`/`Align`/`Position` types exported. Used by Tooltip, DropdownMenu, Popover, Select, Combobox, HoverCard, ContextMenu. **It measures once — always pair it with `useFloatingReposition`** (below) or the surface comes unanchored the moment anything moves.
- **`src/hooks/useFloatingReposition.ts`** — `useFloatingReposition(active, reposition, observeEl?)`; keeps a floating surface glued to its trigger while open. rAF-throttled re-position on window resize + scroll (**capture phase**, so scrolling any ancestor counts) plus an optional `ResizeObserver` on the trigger. `reposition` must be a `useCallback`. Used by Select, Combobox, Popover, DropdownMenu, HoverCard, Tooltip — **one mechanism system-wide; don't hand-roll listeners again.** ContextMenu is the deliberate exception (anchored to a pointer position, so there's no trigger rect to re-measure). Added 2026-07-28 after every surface but Tooltip was found stranding its menu on a panel resize.
- **`src/hooks/usePresence.ts`** — the `closed → open → closing` state machine that keeps a portal mounted through its exit animation. **Promotes to `open` synchronously during render, not in an effect** — a deferred open mounts the content a render late, after the positioning layout-effect already ran against a null ref, leaving it `visibility: hidden` forever. Don't "simplify" that back into a `useEffect`.
- **`src/components/Label/`** — the shared label + description + required-indicator primitive. Any form control (Input, Textarea, Checkbox, future Radio/Switch) should either render a `<Label>` internally or reuse its `.ui-label` classes. Import `../Label/Label.scss` in the component that reuses the classes.
- **`src/components/Input/Input.scss`** — owns `.ui-input-wrap` (border, focus-within ring, hover, error, disabled states). Textarea reuses it via a `--multi` modifier and NativeSelect reuses it as-is (native `<select>` inside the wrap). Future form-field-shaped components (Select trigger, Combobox trigger) should reuse it too rather than redefining borders.

---

## Notable techniques

### Height animation without JS — Collapsible & Accordion pattern

Both Collapsible and Accordion animate open/close height using `grid-template-rows: 0fr → 1fr`. No JS measurement, no ResizeObserver, no framer-motion. Purely CSS.

**The catch:** padding on the inner container leaks into the grid row's minimum-content-size, so a single inner div won't fully collapse to 0 when closed — you get a phantom gap. Fix is a **two-layer clip**:

```tsx
<div className="ui-accordion__content">          {/* grid, 0fr → 1fr */}
  <div className="ui-accordion__content-inner">  {/* overflow: hidden, min-height: 0 */}
    <div className="ui-accordion__content-body"> {/* padding + typography live HERE */}
      {children}
    </div>
  </div>
</div>
```

- The outer `-inner` owns **only** `overflow: hidden` + `min-height: 0` (the clip context)
- The `-body` owns padding + typography — since it's inside the clip context, its padding doesn't contribute to the grid row's min-size

If you build any future component with a similar disclosure animation (Sheet, drawer), use this same 3-div structure.

### Cross-fade between SVG states — Checkbox pattern

The Checkbox's check-mark and indeterminate SVGs are both always mounted (position: absolute overlaying each other) so switching between them cross-fades instead of hard-swapping via `display: none`. The draw-in animation uses `stroke-dasharray: 24; stroke-dashoffset: 24 → 0` on the polyline/line stroke, transitioning with `--ease-spring`. Icons scale from 0.5 → 1 with `--ease-spring-strong` (the animate-ui pop feel). If you're animating an icon that swaps between states, follow this pattern.

### Portal exit animation with a safety-net timeout — Drawer pattern

Tooltip and Drawer keep a `closed → open → closing` state machine so a portal can animate *out* before unmounting (React unmounts instantly otherwise). The panel stays mounted in `closing`, plays its exit keyframes, and unmounts when done. Two hard-won rules for any new component using this:

- **Never put `var()` inside `@keyframes` for the animated property.** A keyframe like `transform: var(--drawer-from)` parks the end value but the browser runs no real interpolation and **never fires `animationend`** — the exit hangs forever. Use explicit literal keyframes (Drawer has per-side `ui-drawer-in/out-{side}`); filter the unmount handler by `animationName.startsWith('ui-drawer-out')` since animation events bubble.
- **Back `animationend` with a duration-based `setTimeout` fallback.** `animationend` doesn't fire under `prefers-reduced-motion`, on a backgrounded/occluded tab, or when an animation is interrupted. For a modal surface (Drawer) that's a stuck-open trap. Drawer's `closing` effect sets a ~400ms timer that forces `state → 'closed'`; whichever of the event or timer fires first wins (both idempotent). **Keep this timeout** — it's the only thing that guarantees the drawer can always close.

---

## Token vocabulary (grep `tokens.scss` before improvising)

**Spacing / sizing** (px in tokens; scale is Tailwind-like):
- `--p-0` (0) → `--p-96` (384px), plus half-steps like `--p-0-5`, `--p-1-5`, `--p-2-5`, `--p-3-5`
- Same scale mirrored for `--w-*` (width) and `--h-*` (height)
- `--max-w-xs` (320px) → `--max-w-7xl` (1280px), plus screen breakpoints

**Typography:**
- `--text-xs` (12px) → `--text-9xl` (128px); use `--text-code: 13px` for inline code
- `--leading-3` (12px) → `--leading-11` (44px)
- `--tracking-tighter` → `--tracking-widest`
- `--font-thin` (100) → `--font-black` (900); most components use `--font-normal` (400), `--font-medium` (500), or `--font-semibold` (600)
- `--font-family: 'Inter', sans-serif` (default), `--font-family-mono: 'JetBrains Mono'`

**Radii / borders:**
- `--rounded-sm` (2px) → `--rounded-3xl` (24px), plus `--rounded-full: 9999px`
- `--border-w-50` (0.5px) → `--border-w-400` (4px)
- `--border` (resting hairline) + `--border-hover` — the strengthened border on hover. Symmetric across modes (light `#64748b` / dark `#cbd5e1`: each mode's hover value is the *other* mode's resting border), so the border always gains contrast on hover. Consumed by Card's `interactive` prop, Item's `--outline` interactive hover, and the shared `.ui-input-wrap` hover (Input/Textarea/NativeSelect).

**Shadows** (theme-aware; each token already embeds its color):
- `--shadow-2xs` (very subtle) → `--shadow-2xl` (dramatic)
- Never write `box-shadow: 0 1px 3px rgba(...)` — always `box-shadow: var(--shadow-md)` etc.

**Effects:**
- `--focus-ring-width: 3px` — the focus ring on every interactive
- `--overlay-blur: 4px` — Dialog backdrop-filter blur
- `--tooltip-slide: 4px` — Tooltip enter/exit translate distance

**The FOUR axes (all attribute-driven, independent, composable):**
- **Mode = light/dark**, set via **`data-mode='light'` / `data-mode='dark'`** on `<html>`. Tokens swap on `:root, [data-mode='light']` and `[data-mode='dark']`. The `ModeToggler` component owns this attribute.
- **Theme = a sub-brand**, set via **`data-theme='{code}'`** on `<html>` or any subtree. **SIX brands ship: `db dc ec nb ph rm`** — `dr` and `ir` were retired 2026-08-08 (see Recent decisions). A theme remaps the `--primary` family AND, since deeper theming v2, brings `--primary-deep`, the artwork pair `--decorative-hi` / `--decorative-deep`, a composed `--decorative-gradient`, and its own `--chart-1..6` + `--chart-muted`. The **main brand is the absence of `data-theme`** (`--primary` stays neutral slate). Do NOT reintroduce a `--brand` token or a `variant="brand"` — that model was removed.
- **Surface = the Aiden AI surface**, set via **`data-surface='aiden'`** on a panel/subtree. It is **NOT a theme code** (never in the brand picker) — it's a cross-cutting identity that layers *inside* any brand. It deliberately **re-pins the 13 neutral surfaces to `var(--base-*)`** so a tinted host page cannot leak into an Aiden panel. See the Aiden entry in Recent decisions and `Foundations/Themes → Aiden Surface`.
- **Tint = brand-tinted neutrals**, set via **`data-tint='page rail'`** on `<html>` or a subtree (whitespace list — `page`, `rail`, or both). **OPT-IN: absence means neutral**, and that is guaranteed rather than conventional — `--tint-page` / `--tint-rail` default to `0` and every surface is `color-mix(…, calc(N% * var(--tint-page, 0)), var(--base-*))`, which at 0 collapses numerically to the base. `page` tints the content surfaces, `rail` tints the sidebar alone. Light does NOT tint `--background`/`--card`/`--popover`; dark does. Full record in **`docs/deeper-theming-v2-merge.md`**.

**Deeper theming v2 — the three rules you will break if you don't know them:**
1. **A custom property cannot reference itself.** `--card: color-mix(…, var(--card))` is a cycle that resolves to `unset` — not to the old value, and with **no error**. That is why the 13 tintable surfaces hold their literal in a `--base-*` twin (`--card: var(--base-card)`) and brand scopes mix against `--base-card`. `--base-*` is the input to the tint derivation, **not** a second public vocabulary — don't read it from components.
2. **A `var()` inside a custom property resolves on the element that DECLARES it**, not where it's used. `--tint-stock` derives from `--primary-deep`, which only a brand scope has, so the tint derivations MUST live under `[data-theme]`. In a mode block they'd resolve against nothing and go invalid.
3. **Light is declared AFTER dark** in the tint + generated blocks. Both are 2-attribute compounds, so a light subtree inside a dark page matches both and the tie breaks on source order — light has to come last to win. Plain dark is unaffected (the light rule's bare `[data-theme]` member is 1 attribute and loses on specificity).

**The brand token blocks in `tokens.scss` are GENERATED — do not hand-edit them.** They live between `/* @generated … */` markers and come from `src/prototypes/deeperThemingRecipeV2.ts` via `npm run tokens:gen`. `npm run test:tokens` fails the build on drift. Editing the values in `tokens.scss` directly will be reverted on the next run; change the recipe instead.

**Semantic colors (all mode-aware):**
- **`--primary-main` / `--primary-main-foreground`** — the MAIN brand's primary, held apart from `--primary` so it survives theming. **No `[data-theme]` scope remaps them**, which makes them the escape hatch for chrome that must stay neutral inside a themed subtree. `--primary` is now sourced from `--primary-main` at `:root` (one literal, not two) and is what a theme moves. First consumer: **`.ui-alert__action`**, so an Alert's action button reads as the alert's own control rather than competing with the page's themed CTA. **Rebuild the WHOLE `--primary-*` family when you use them** — a theme scope re-declares every derived token, so overriding `--primary` alone leaves `-soft`/`-border`/`-text` themed and yields a slate fill with a brand-coloured border. **Scope it to the part, not the block:** Alert's `brand` variant reads `--primary-light`/`-text`/`-border` on the alert *surface* and is supposed to theme, so declaring the override on `.ui-alert` would silently delete the only themed Alert.
- Core: `--background`, `--foreground`, `--primary`, `--primary-foreground`, `--secondary`, `--secondary-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--card`, `--card-foreground`, `--popover`, `--popover-foreground`, `--border`, `--input`, `--ring`, `--focus`
- **`--avatar-background` / `--avatar-foreground`** — the fallback initials disc, a SOLID slate chip rather than a tint, because it has to read as an object on whatever surface it sits on. Light **slate-500**, the lightest step clearing 3:1 against every surface an avatar can land on (worst 3.21 vs `--muted`). Dark **INVERTS to slate-300** — not a style choice: dark `--popover` *is* slate-600, so a slate-500 disc sits one step away at 1.59:1 and vanishes in a HoverCard. Replaced `--secondary`, which measured **1.23:1 on `--card` in light and 1.00:1 in dark** — the disc was invisible on the two surfaces avatars most often appear on. Avatar's **count badge stays on `--muted`** (no small text on it, and it wants the weight).
- `--tooltip-background` / `--tooltip-foreground` — neutral slate inverse, **never remapped by a theme** (Tooltip is the one carve-out from theming).
- Semantic families: `--error`, `--error-foreground`, `--error-light`, `--error-soft`, `--error-border`, `--error-hover`, `--error-ring`, `--error-focus`. Same shape for `--success`, `--warning`, `--info` — and `--primary` carries the same derived family (`--primary-light/-soft/-border/-hover/-ring/-focus/-text`), which theme scopes remap.
  - **`--primary-text` is the "on-surface" primary — use it whenever `--primary` is *text* on a light/subtle surface** (outline / ghost-is-neutral-so-not-it / link / secondary button text, Badge `outline`, brand Alert/Banner titles). It's `color-mix(in srgb, var(--primary) 85%, var(--foreground))` — nudges the theme colour toward the mode's text colour (darker in light, lighter in dark) so mid-luminance themes clear AA as text (worst case 5.2 light / 5.8 dark). **Solid fills keep raw `--primary`** (they pair with `--primary-foreground`, which already passes). Don't use `--primary-text` for fills, borders, or the neutral ghost button.
  - **`-light` vs `-soft`:** `-light` is the *subtle* tint (6%) for surfaces — Alert backgrounds, destructive menu rows, ghost-hover — and was tuned for WCAG-AA text-on-tint. (Badge `*-outline` variants are transparent now, so `-light` no longer backs any Badge.) `-soft` is the *visible* tint used **only** for the filled `secondary` button, so `secondary` (filled tint + border) reads distinctly from `outline` (transparent + border). Don't swap them: bumping `-light` would over-saturate Alerts and break the AA math.
  - **`-soft` is mode-split for contrast:** **8% in light**, **10% in dark**. Main-brand + semantic `-soft` are split in the base `[data-mode]` blocks; the themed `--primary-soft` is split by a `[data-mode='light'] [data-theme]` rule near the bottom of `tokens.scss` (2-attr specificity beats the single `[data-theme='{code}']` scope). Aiden's `--aiden-secondary` fill is a matching low-alpha gradient (8% light / 10% dark), not a solid hex. Re-run the contrast script if you touch these percentages.
- Aiden (the AI **surface**, `data-surface='aiden'`): `--aiden-primary` (the violet→blurple→blue **gradient** fill), `--aiden-hover` (hover gradient — deeper in light, *lighter* in dark since fills carry dark text there), `--aiden-outline-border` (the **solid "blurple"** `#5a37e6` light / `#9076f9` dark = the surface's `--primary`), `--aiden-secondary`/`-border`/`-ring`/`-focus` (tints for the standalone `variant="aiden"`). Mode-aware. Aiden secondary hover uses the shared `--opacity-50` overlay like every other secondary button (lightens in light, darkens in dark) — no bespoke aiden hover token. See the Aiden entry in Recent decisions for how the surface scope works.

**Theme scopes** (`src/styles/tokens.scss`, bottom): `[data-theme='{code}'] { --primary: var(--{code}-primary); --primary-foreground: var(--{code}-primary-foreground); }` for `db, dc, dr, ec, ir, nb, ph, rm`. The per-theme colors live in the "Theme Palettes" blocks (mode-aware). Consumers just wrap a subtree: `<section data-theme="db">…</section>` — every `--primary` consumer inside picks up the color, no component changes.

**How `--primary` reaches components (full immersion, except Tooltip):** primary button (solid `default` variant), the `default` variant's soft `secondary` / `outline` / `link` styles, checked Checkbox, Radio dot, Switch track, active Chip, Toast action, Progress default indicator, InputGroup, and the Badge `default`/`outline` variants — all read `--primary` and theme automatically. **The `default` variant's `ghost` style is the deliberate exception — it stays neutral slate (`--secondary-foreground` / `--accent`) so quiet companion actions like a Dialog "Cancel" never compete with the themed CTA.** `--secondary`/`--muted`/`--border`-based chrome and Tooltip also stay neutral.

**When a user names a theme code — the routing rules Claude should apply:**

| Role | Component + props | Themes? |
|---|---|---|
| Primary CTA | `<Button variant="default" />` inside `data-theme` | ✅ solid theme color |
| Soft / outline / link action | `<Button style="secondary" \| "outline" \| "link" />` | ✅ theme color (soft tint, border, or link accent) |
| Quiet / cancel | `<Button style="ghost" />` | ❌ neutral slate (the one carve-out in the default family) |
| Destructive | `<Button variant="error" />` | ❌ stays red regardless of theme |
| Status pill / badge | `<Badge variant="default" \| "outline" />` | ✅ (both theme — `default` = solid `--primary` fill, `outline` = `--primary-border`; semantic `error`/`*-outline` etc. keep their own color) |
| Checkbox / Switch / Radio (checked) | as-is | ✅ theme color |
| Card / panel chrome, borders, body text | neutral tokens | ❌ neutral |
| Tooltip | `<Tooltip>` | ❌ always neutral (carve-out) |

**Rule of thumb:** `--primary` = the current theme's color (slate on the main brand). Wrap a subtree in `data-theme="{code}"` to swap the accent; neutral tokens don't move. See `Foundations / Themes` in Storybook.

**Category colors** (chart / tags): `--category-red`, `--category-blue`, `--category-emerald`, `--category-violet`, etc. (15 hues — yellow + lime were removed). Per hue: `-bg` (tint: 0.1 light / 0.15 dark), `-hover` (interactive state — **currently unused**, kept as the documented complete-palette token), and `-text`; plus one global `--category-foreground`. Roles: **`--category-{c}` = the vivid fill (charts / dots / *solid* badge)**; **`--category-{c}-bg` = the soft-tag tint**; **`--category-{c}-text` = AA-safe text ON that tint** (light darkened past AA, dark reuses base); **`--category-foreground` = white in light / `#0f172a` in dark** (inverted, exactly like `--error-foreground`) — text on the vivid solid badge. **Owner chose white and accepts it dips under AA on the lighter hues** (amber/green ~2.1–2.6 in light); the soft `-text` variant is the AA-safe path. **No `-solid` fill token** — the solid badge reads the vivid base directly. **Never use raw `--category-{c}` as small text on its tint — use `-text`.** `test:contrast` AA-gates the 15 soft `-text`-on-`-bg` pairings only; the solid badge is intentionally best-effort, not gated.

**Sidebar surface** (Sidebar-only chrome palette, mode-aware): `--sidebar`, `--sidebar-foreground`, `--sidebar-primary`(`-foreground`), `--sidebar-accent`(`-foreground` — hover/active menu rows), `--sidebar-border`, `--sidebar-ring`. Plus width constants `--sidebar-width` (16rem), `--sidebar-width-icon` (3rem), `--sidebar-width-mobile` (18rem). Not remapped by a `data-theme` (it's neutral chrome, like Tooltip).

**Mark glass** (`Mark`-only, mode-aware): `--mark-lift`, `--mark-shade`, `--mark-bloom`, `--mark-sheen`, `--mark-spark`, `--mark-sweep`, plus shadow colours `--mark-shadow-cast`(`-lifted`), `--mark-shadow-occlusion`, `--mark-shadow-rim`(`-lifted`), `--mark-shadow-edge`, `--mark-glyph-shadow`. These are **whole gradients and whole shadow colours**, not a hex plus an alpha — same reasoning as `--shadow-*`: the layer IS the value, and splitting a four-stop falloff into four tokens lets a consumer set three and get something that is no longer a highlight. Four flip in dark (`-lift`, `-shade`, `-sheen`, `-glyph-shadow`) because the lighting model turns over rather than darkening — see the Mark entry in Recent decisions. Plus **`--mark-ramp`**, the main brand's stand-in for `--decorative-gradient`; read the note on it before touching it.

**Motion:**
- Duration (transitions): `--duration-instant: 150ms` (micro hover shifts), `--duration-fast: 100ms`, `--duration-normal: 200ms`, `--duration-slow: 300ms`
- Duration (loops): `--duration-spin: 900ms` (Spinner), `--duration-shimmer: 1400ms` (Skeleton/Progress/Attachment)
- Easing (native): `--ease-default`, `--ease-in`, `--ease-out`, `--ease-in-out`, `--ease-linear`
- Easing (spring): `--ease-spring` (smooth, no overshoot) — was the Tooltip literal; `--ease-spring-strong` (slight overshoot, animate-ui-flavored pop)
- Slide distances: `--motion-slide-sm: 4px`, `--motion-slide-md: 8px`, `--motion-slide-lg: 12px`; enter scale: `--motion-scale-in: 0.97`
- `--tooltip-slide` is a back-compat alias for `--motion-slide-sm` — prefer the generic name in new code

---

## Component roster (as of this file's last update)

Each is exported from `src/index.ts`. See the individual `.tsx` for full prop signatures.

| Component | Compound? | Notes |
|---|---|---|
| `Accordion` + `AccordionItem` + `AccordionTrigger` + `AccordionContent` | yes | `type` = `single` (+ `collapsible`) or `multiple`. Height animation via the 3-div `grid-template-rows: 0fr→1fr` clip (see Notable techniques) |
| `Alert` | no | `variant`-driven semantic banner; dismissal X composes `CloseButton` |
| `AlertDialog` + `AlertDialogHeader` + `AlertDialogBody` + `AlertDialogFooter` | yes | Confirmation dialog. A thin **preset of `Dialog`** (role=`alertdialog`, no overlay-click-close, no header X) — reuses Dialog's portal/focus-trap/escape/restore + Dialog.scss; only new Dialog surface is an optional `role` prop |
| `AspectRatio` | no | CSS `aspect-ratio` wrapper; `ratio` passed as a division expression (`16 / 9`). Media children fill+cover. Layout primitive, no `id` |
| `Attachment` (+ `Media` `Content` `Title` `Description` `Actions` `Action` `Trigger` `Group`) | yes | 9 exports. File-attachment row/tile; `size` = xs/sm/default, `orientation` = horizontal/vertical, `AttachmentMedia variant` = icon/image. Shimmer via `--duration-shimmer`. Parallel family to `Item` |
| `Avatar` + `AvatarGroup` | yes | Fallback initials + optional image + status-dot badge |
| `Badge` | no | 12 variants — every color is `{solid base, transparent outline}`; no brand — default/outline theme via `--primary`. Plus `category?: CategoryColor` + `categoryStyle?: 'soft' \| 'solid'` (17-hue tag: soft `-bg`+`-text` or solid `--category`+`-foreground`; overrides `variant`). Extends `HTMLAttributes<HTMLDivElement>` (full `...rest`) |
| `Banner` | no | Page-level full-bleed announcement bar (distinct from inline `Alert`); reuses Alert's semantic tint tokens + bottom hairline; composes `CloseButton` (dismiss) + `Button` (action); `role` alert/status by severity; `centered` |
| `Blockquote` | no | Semantic `<blockquote>` + accent border + italic body; optional `cite` renders a `<footer>` with em-dash. Native `cite` URL attr Omit-ed and redefined as content |
| `Button` | no | 6 variants × 5 styles × **4 sizes** (`xs`/`sm`/`default`/`lg`) + `isLoading` (no brand — `default` IS the theme's primary) |
| `Breadcrumb` (+ `List` `Item` `Link` `Page` `Separator` `Ellipsis`) | yes | 7 exports. Links use `text-underline-offset: 3px`; `Ellipsis` carries an sr-only label |
| `ButtonGroup` + `ButtonGroupSeparator` + `ButtonGroupText` | yes | Corner-flatten via CSS `:not(:first/last-child)` |
| `Card` + `CardHeader` + `CardMedia` + `CardBody` + `CardFooter` | yes | Header prop-driven (`title`, `description`, `media`, `action`). **Two different media roles, don't confuse them:** `CardHeader.media` is a LEADING visual on the title's own line (avatar, status dot, featured icon); **`CardMedia` is the full-bleed COVER** — image/video/chart — placed by the caller so it can sit above the header, between header and body, or last. `CardMedia` composes `AspectRatio` (`ratio`, default `16 / 9`) so a grid keeps a level top edge and nothing shifts as images load, and it declares **no radius of its own** — `.ui-card` is `overflow: hidden`, so a cover touching an edge inherits the card's rounding; adding one shows as a hairline of card background in the corners |
| `Chat` (+ `ChatMessageList` `ChatMessage` `ChatBubble` `ChatMessageActions` `ChatMarker` `ChatComposer` `ChatComposerInput` `ChatComposerActions` `ChatComposerSend` `ChatComposerDictation` `ChatComposerDrawer` `ChatToolCalls` `ChatToolCall` `ChatSuggestions` `ChatSuggestion` `ChatLayout` + `Header`/`Body`/`Footer`) | yes | Aiden AI chat family. Root provides `density` via context; `ChatMessage from` (user/assistant/system) shared to descendants + drives alignment; **Claude-style transcript** — assistant = full-width bubble-less prose, user = contained bubble on the right (uniform corners, no tail), centered `max-w-3xl` column; `ChatBubble` `pending` typing dots + `streaming` blinking caret; `ChatMarker variant` divider/system/status (composes `StatusDot`); composer reuses `.ui-input-wrap` + autosize textarea (Enter sends / Shift+Enter newline), send composes `Button variant="aiden"` (send↔stop on `isStreaming`/`onStop`); `ChatToolCall` collapsible agent-action card (composes `Collapsible`, status running/success/error via `Spinner`/icons, args/result via `Code`); `ChatSuggestion` one-shot prompt pill (momentary button, not `Chip`); `ChatComposerDictation` mic toggle (presentational `recording` → red + pulse); `ChatComposerDrawer` expands the composer into a `Drawer` (bound to the same composer value); `ChatLayout` = full-height page shell (fixed header / scrollable body / docked composer footer, provides `density`); `ChatMessageEdit` inline editor (Enter save / Esc cancel), `ChatMessageVersions` `‹1/3›` pager, `ChatReasoning` collapsible "thinking" block (composes `Collapsible`), `ChatCitation` inline marker + `ChatSources`/`ChatSource` source cards, `ChatGreeting` centered start screen. Attachments reuse `Attachment`. Content is consumer-provided (no Markdown dep); `useStreamingText` gives the type-out reveal |
| `ChatMarkdown` | no | **The `@ui/lib/markdown` subpath, NOT the main entry** — the one component allowed the approved deps (`react-markdown`+`remark-gfm`+`highlight.js`, hard rule 1's scoped exception). Renders model markdown through OUR primitives (fences→`CodeBlock` with hljs on the `--code-*` palette, inline→`Code`, quotes→`Blockquote`); raw HTML stays inert (never add `rehype-raw`) |
| `AidenPanel` + `AidenPanelHeader` | yes | **Non-modal** assistant side panel — `complementary` landmark, no trap/lock/overlay; panel-scoped Escape; **`--z-40`, deliberately below the floating surfaces at 50** so menus opened inside paint above it; Drawer's exit machine + 400ms fallback; `--aiden-panel-width` (420px) + `--ui-aiden-panel-inset-top` hook; full-bleed <768px; self-applies `data-surface` |
| `AidenFullScreen` | no | Full-screen Aiden — a `FullScreenDialog` preset (`contentWidth="full"` + the new `FullScreenDialogBody.flush`, which hands scroll to the `ChatLayout` child); self-applies the surface; compact header via a 3-class override |
| `AidenLauncher` | no | The embedded ask (the DART Central doorway pattern): `ChatGreeting` + composer + suggestions; `onSubmit(value)` is the whole contract — it never routes; suggestion click SUBMITS; owns its draft unless controlled |
| `Checkbox` | no | Native input + custom visual + `indeterminate` prop; `size` sm/default/lg (scales box + check + label) |
| `Chip` | no | Toggle-style; `active` state uses `--primary` |
| `CloseButton` | no | Uses `X` from `lucide-react` |
| `Code` + `CodeBlock` | no | Inline `Code` (`<code>`) + fenced `CodeBlock` (`<pre>` + optional filename header + copy `Button` via `navigator.clipboard`, icon/label swap to check/"Copied"). No syntax highlighting (needs a dep). One folder, two exports |
| `Collapsible` + `CollapsibleTrigger` + `CollapsibleContent` | yes | Same `0fr→1fr` grid height animation as Accordion; single disclosure |
| `Combobox` | no | Single export (not compound). Filterable listbox on `computePosition`; `size`/`side`/`align` |
| `Command` (+ `Dialog` `Input` `List` `Empty` `Group` `Item` `Separator` `Shortcut`) | yes | 9 exports. Command palette; `CommandItem variant` = default/error. Max-height 480px / dialog 560px (panel geometry). One of the 4 menu-item styling copies |
| `ContextMenu` (+ 13 parts incl. `Sub`/`SubTrigger`/`SubContent`) | yes | 14 exports — the only menu family with submenus. `variant` = default/destructive; `side`/`align` via `computePosition` |
| `Dialog` + `DialogHeader` + `DialogBody` + `DialogFooter` | yes | Portal + focus trap + scroll lock + Escape |
| `Drawer` + `DrawerHeader` + `DrawerBody` + `DrawerFooter` | yes | Edge panel (`side` = top/right/bottom/left, default right); reuses Dialog's portal/focus-trap/scroll-lock/Escape + Tooltip's `closed→open→closing` exit state machine for the slide-out. `open`/`onClose` controlled, no drag |
| `FullScreenDialog` + `Header` + `Body` + `Footer` | yes | The page-over-a-page — Help, Feedback, settings. **A preset of `Dialog`** (AlertDialog technique): no new portal/focus-trap/scroll-lock/exit machine, and Header/Body/Footer are Dialog's chrome. Edge-to-edge, no rounding, Dialog's centre-scale motion (**no new keyframes**). X + Escape only, **no click-outside** — a stray click must not discard a form. `contentWidth` md/lg/xl/full caps the body's content column via a **centred grid track**, so the body stays the full-width scroll container and its scrollbar hugs the viewport. Owns Escape itself (`Dialog.closeOnEscape={false}`) so a nested Select/menu gets the Escape first |
| `DirectionProvider` + `useDirection` | no (provider) | RTL/LTR provider. Sets `dir` on a `display:contents` (layout-neutral) wrapper and shares the direction via context; `useDirection()` reads it. Components using CSS logical properties + flex rows mirror automatically under `dir="rtl"` — no component changes needed. Mirrors `SidebarProvider`/`useSidebar` naming |
| `Empty` + `EmptyHeader` + `EmptyMedia` + `EmptyTitle` + `EmptyDescription` + `EmptyContent` | yes | Centered empty-state; static (mirrors Item's compound pattern). `EmptyMedia variant` = `icon` (boxed muted tile) or `default` (unboxed) |
| `Fab` | no | Viewport-pinned floating action button — the always-available launcher ("Ask Aiden"). `position` = bottom-right/bottom-left/top-right/top-left, `size` = default/lg, optional `pulse` halo rings and a `badge` slot. Carries the Aiden gradient by default; the pulse is suppressed when disabled |
| `FeaturedIcon` | no | The boxed icon tile — a lucide glyph on a tinted, bordered surface. `size` = sm/default/lg, `shape` = square/circle, `variant` = default/brand/success/warning/error/info. Extracted from `Empty`, which now composes it rather than hand-rolling its own tile |
| `DropdownMenu` + `Trigger` + `Content` + `Item` + `Label` + `Separator` + `Group` + `CheckboxItem` + `RadioGroup` + `RadioItem` + `Shortcut` | yes | 11 exports; keyboard nav wraps at ends |
| `Mark` | no | The **application mark** — the brand's `--decorative-gradient` tile under a stack of glass, optionally locked up with the app's name (`title` + `description`). The only chrome carrying a brand's FULL three-anchor identity rather than just `--primary`. `Icon: LucideIcon`, `size` = sm/default/lg/xl, `motion` = none/ambient/tilt (default `ambient`; `tilt` uses `usePointerTilt`). **No `brand` prop** — it reads the `data-theme`/`data-surface` it stands in. Glass literals live in tokens.scss as `--mark-*` |
| `Menubar` (+ `MenubarMenu` `MenubarTrigger` `MenubarContent` `MenubarItem` `MenubarLabel` `MenubarSeparator` `MenubarGroup` `MenubarCheckboxItem` `MenubarRadioGroup` `MenubarRadioItem` `MenubarShortcut`) | yes | Desktop-style horizontal menu bar. **Reuses `DropdownMenu` wholesale** — each `MenubarMenu` is a *controlled* `DropdownMenu` (open state driven by the bar), and the menu-surface parts are DropdownMenu's re-exported under Menubar names (so positioning / item-nav / escape / outside-click all come free). Menubar adds: single-open coordination, hover-to-switch between menus once one is open, roving-tabindex + ArrowLeft/Right across triggers. `MenubarMenu value` = the menu's identity. No submenus / no in-menu Left/Right yet |
| `Field` (+ `Set` `Legend` `Group` `Content` `Label` `Title` `Description` `Error` `Separator`) | yes | 10 exports. Form-row scaffolding; `orientation` prop; label typography reuses the shared `.ui-label__*` classes, `FieldSeparator` wraps `Separator` |
| `HoverCard` + `HoverCardTrigger` + `HoverCardContent` | yes | Hover-intent popover on `computePosition`; `--popover` surface |
| `Input` | no | `label` composes `<Label>`; `IconLeft`/`IconRight` slots; `error` + `errorMessage` |
| `InputOTP` | no | Segmented one-time-code input. **Single real `<input>` holds the whole value** (native paste / mobile `one-time-code` autofill / selection), visually rendered as `length` slots; active slot = the caret position, with a blinking caret in the active empty slot. `groupSize` inserts a separator (e.g. 3 → 3·3); `pattern` (default digits) filters allowed chars; `onComplete` fires at full length; `label` composes `<Label>`; `error`+`errorMessage`, `size` sm/default/lg |
| `InputGroup` (+ `Input` `Textarea` `Addon` `Text` `Button`) | yes | 6 exports. Addon-flanked field; reads `--primary`; carries the `1000px` autofill box-shadow kill-switch |
| `Item` (+ `Group` `Separator` `Media` `Content` `Title` `Description` `Actions` `Header` `Footer`) | yes | 10 exports. Generic list row; `variant` = default/outline/muted, `size` = xs/sm/default, `ItemMedia variant` = default/icon/image. Parallel family to `Attachment` |
| `ModeToggler` | no | Owns the `data-mode` light/dark attribute on `<html>`; `variant` = default/outline/ghost, `size` = sm/default/lg |
| `NativeSelect` + `NativeSelectOption` + `NativeSelectOptGroup` | yes | Styled native `<select>` (distinct from the floating `Select`); composes `<Label>` + reuses Input's `.ui-input-field`/`.ui-input-wrap`; native chrome stripped, custom chevron overlaid; `size`/`error`/`required` like Input |
| `Kbd` | no | Styled `<kbd>` keycap, mono + raised bottom border; `size` = sm/default/lg |
| `Label` | no | Shared with Checkbox internal label + Input/Textarea's built-in label; `required` renders `*` in `--error` |
| `Popover` + `PopoverTrigger` + `PopoverContent` + `PopoverClose` | yes | `computePosition` floating surface (`--popover`); `side`/`align`, no collision detection |
| `Pagination` (+ `Content` `Item` `Link` `Previous` `Next` `Ellipsis`) | yes | Page-nav compound; cells reuse `.ui-button` (ghost, outline+`aria-current` when active); imports Button.scss so cells style in isolation; mirrors Breadcrumb ellipsis |
| `Progress` (+ `Label` `Value` `Track` `Indicator`) | yes | 5 exports. `size` = sm/default/lg, `variant`, `indeterminate` (shimmer via `--duration-shimmer`); default indicator reads `--primary` |
| `RadioGroup` + `RadioGroupItem` | yes | `orientation` prop; checked dot uses `--primary`; `size` sm/default/lg on the group (scales every item's circle + dot + label from the root class, no context needed) |
| `ScrollArea` | no | Custom JS thumb, grid layout, no floating-ui dep |
| `Select` (+ `Trigger` `Content` `Item` `Group` `Label` `Separator`) | yes | 7 exports. Floating listbox (distinct from `NativeSelect`); `size`/`side`/`align`. One of the 4 menu-item styling copies |
| `Separator` | no | `orientation` prop; the canonical hairline-with-label primitive (`FieldSeparator` wraps it) |
| `Sidebar` (+ `SidebarProvider`, `useSidebar`, ~21 parts) | yes | Full app-sidebar subsystem. `collapsible` = offcanvas/icon/none, `variant` = sidebar/floating/inset, `side` = left/right. ⌘B shortcut, localStorage persistence, mobile (<768px) renders as a `Drawer`, icon-mode `tooltip` via `Tooltip`, `SidebarMenuSkeleton` via `Skeleton`. Own `--sidebar-*` token surface. Collapse = in-flow gap spacer + viewport-fixed panel driven by `data-state`/`data-collapsible` |
| `Skeleton` | no | `shape` = default/circle/text; shimmer via `--duration-shimmer` |
| `Slider` | no | Single value, or `range` for two thumbs (`[lower, upper]`). `min`/`max`/`step`, `showValue`, `formatValue`, `size` = sm/default/lg. Each thumb is a `role="slider"` span whose `aria-valuemin`/`max` stop at its neighbour. Composes `<Label>`; strips its own union props from `...rest` like Accordion |
| `Spinner` | no | lucide `LoaderCircle` + `--duration-spin` rotation; `role="status"` |
| `StatusDot` | no | Presence dot (online/offline/busy/away/neutral) → semantic tokens via `--ui-status-color`; optional `pulse` ring (reduced-motion aware); `role="status"`+label when named. Mirrors Avatar badge |
| `Switch` | no | `size` prop; checked track uses `--primary`. Thumb travel distances (12/16/20px) are intentional component-internal literals |
| `TabBar` + `TabBarList` + `TabBarTab` + `TabBarNewTab` | yes | The **browser-style strip of OPEN DOCUMENTS** — not `Tabs`. A tab stands for something the user opened and can close; a `Tabs` trigger reveals a panel shipped beside it. Root holds `value`/`onValueChange`, `TabBarList` is the scrolling `role="tablist"`, `TabBarNewTab` (the `+`) sits **outside** it. **Activation defaults to `manual`** (inverted from `Tabs`) — arrowing across five tabs would mount five dashboards. A tab is a `div[role="tab"]`, **not a `<button>`**, so the close `<button>` can nest legally; Enter/Space are handled by hand as a result. Closes nothing itself — `onClose` reports intent |
| `Tabs` + `TabsList` + `TabsTrigger` + `TabsContent` | yes | `orientation` prop; `variant` = default/line/browser (presentation only — identical semantics, keyboard and aria). `default` list is `--accent`; `line` marks the active tab with a `--primary` bar (themes); `browser` makes the active tab a `--card` tab joined to its panel, which gains its own surface — **don't nest a `Card` in a browser panel**. `browser` is horizontal-only and falls back to `line` when vertical. **Looking like TabBar is not being TabBar** — see the routing rule below |
| `Textarea` | no | Reuses Input's `.ui-input-wrap` via `--multi` modifier |
| `Toast` — `Toaster` + `toast()` | yes | **Imperative API, not a compound tree**: render `<Toaster>` once, call `toast()` anywhere. `variant`, `duration`, `position`. 380px stack / 300px card min-width (panel geometry); action reads `--primary`; dismissal X composes `CloseButton` |
| `Tooltip` + `TooltipTrigger` + `TooltipContent` | yes | State machine (`closed`/`open`/`closing`), direction-aware animations, portal |
| `Toggle` | no | `aria-pressed` squared two-state button; controlled (`pressed`) or uncontrolled (`defaultPressed`); pressed = neutral `--accent` fill. Distinct from `Chip` (rounded filter pill); reuses its interaction shape |
| `ToggleGroup` + `ToggleGroupItem` | yes | Segmented control; `type` single (re-click clears) / multiple; items reuse `.ui-toggle` + collapse shared borders (ButtonGroup-style). Strips union props from `...rest` like Accordion |

---

## How to build a new component (from scratch)

1. **Check a reference implementation for the shape:** read the component's API + visual spec + state matrix from whatever reference design system you're mirroring. **Do not copy code** (references typically use Tailwind + Radix, we don't) — only extract the shape.
2. **Create the folder** at `src/components/{Name}/` with the exact file structure above.
3. **Types file first** — define `{Name}Props` extending the right `React.*HTMLAttributes<...>`, `Omit` any collisions, add our specific props.
4. **Component:** `forwardRef` + `displayName` + `className` merge + `...rest` spread. Import `./Name.scss` at the top.
5. **SCSS:** BEM classes only, tokens only. Reference Tooltip.scss or Card.scss for style patterns.
6. **Stories:** `Playground` (Storybook controls), `AllVariants` or `AllStates` (matrix), plus state-specific stories.
7. **Wire into `src/index.ts`** as `export { default as {Name} } from './components/{Name}';` plus its types.
8. **Verify:** `npx tsc --noEmit`, boot Storybook, screenshot each new story. Confirm existing stories didn't regress.

---

## How to modify an existing component

Same rules apply. Before editing:
- Run `npx tsc --noEmit` to confirm the current baseline is clean
- Boot Storybook and screenshot the affected component's stories (visual baseline)
- Make the change
- Re-screenshot and diff mentally against the baseline
- If a story changes unintentionally, either revert or update the story to match the new intent

---

## Verification workflow

```bash
# Typecheck
npx tsc --noEmit

# Boot Storybook (dev)
npm run storybook       # port 6006 by default

# Build the library (verifies the lib bundle actually compiles)
npm run build

# WCAG contrast guardrail (fails below AA on any text-on-surface pairing)
npm run test:contrast

# Grep guardrails
grep -rE "#[0-9a-fA-F]{3,8}" src/components/   # should be empty
grep -rE "rgba\("              src/components/   # should be empty
grep -rE "^import.*from"       src/components/ | grep -oE "from ['\"][^'\"]+['\"]" | sort -u
# ↑ only 'react', 'react-dom', 'lucide-react', '@storybook/react', or relative/alias imports allowed
# — plus, ONLY under src/components/ChatMarkdown/: 'react-markdown', 'remark-gfm', 'highlight.js/*'
#   (the scoped exception in hard rule 1; anywhere else these are a violation)
```

If any of the greps produces output that doesn't fit the "allowed" list, you've introduced something that violates the rules above.

---

## Design decisions the user has made — do not relitigate

- **Choosing between `Select` / `NativeSelect` / `Combobox` (they look identical when closed — pick by behavior, not looks).** A static mockup can't tell them apart; the choice is a behavior decision:
  - **`NativeSelect`** — the default. Reach for it first for short, simple option lists (≤ ~7), especially on mobile. Renders the OS-native `<select>` (native scroll wheel, best a11y, cheapest). No custom item rendering, no search.
  - **`Select`** — the styled floating listbox. Use it when you need rich items (icons, descriptions, groups, separators) or pixel-consistent styling across platforms. No built-in search.
  - **`Combobox`** — the only one with a **search field**. Use it when the list is long (timezones, countries, users) or the user should filter by typing.
  - Rule of thumb: **NativeSelect first → Select for rich items → Combobox for long/searchable lists.** (Surfaced by the Settings handoff test-drive: all three render as an identical bordered field + chevron, so a design can only communicate the pick via the layer name — which is the argument for composing real component instances in designs, not hand-drawn boxes.)
- **Choosing between `Tabs` and `TabBar` (pick by what a tab STANDS FOR — they can now look identical, deliberately).** `Tabs` gained `variant="browser"` on 2026-08-26, which borrows TabBar's shape. That was an owner decision, made knowing it blurs the visual line, so the rule is now purely behavioural and has to be stated rather than seen:
  - **`Tabs`** — the panels ship *with* the component. A trigger reveals content that is already there, the set is fixed by the developer, and nothing can be closed. Any of the three variants; `browser` when the panel is its own document-like surface.
  - **`TabBar`** — a tab stands for a document the *user* opened, and can close. It owns `onClose`, overflow scrolling, and `activationMode="manual"` by default (arrowing across five tabs would mount five dashboards). Its tab is a `div[role="tab"]`, not a `<button>`, so the close `<button>` can legally nest inside.
  - Rule of thumb: **fixed panels shipped beside the trigger → Tabs (any variant) · a strip of user-opened, closable documents → TabBar.** If you find yourself wanting a close button on a `Tabs` trigger, you wanted `TabBar`.
  - This is the same class of gap as the Select and Chip clusters below: **a static design cannot tell them apart at all now**, so a mockup must communicate the pick in the layer name or by composing the real component.

- **Choosing between `Chip` / `Toggle` / `ToggleGroup` (pick by selection model, not looks).** All three are "pressable pills/buttons"; the difference is how many can be on at once:
  - **`Chip`** — independent, **multi-select** filter/tag pills. Use for "filter by any of these tags" where zero-or-more can be active at once. Each Chip is its own on/off.
  - **`Toggle`** — a **single** standalone on/off button (`aria-pressed`), like a bold/italic button or a "show archived" switch-as-button. One thing, two states.
  - **`ToggleGroup`** — a **segmented single-select** (or explicit multi): one-of-N mutually-exclusive options sharing a border, like a view switcher (list/grid) or a status filter (All/Active/Invited). Reach for this whenever exactly one option is selected at a time.
  - Rule of thumb: **many-on → Chip · one on/off → Toggle · one-of-N → ToggleGroup.** (Surfaced by the Team-list handoff test-drive: a mutually-exclusive status filter was drawn as Chips, but exclusive selection is really a ToggleGroup — the static design can't show the selection model, so this is the same class of gap as the Select cluster.)
- **A dismissal action is `style="ghost"` — except in Toast, which is a recorded carve-out (2026-08-25).** Cancel / Discard / Decline / Keep sit beside a themed CTA, and in the `default` variant `outline` reads `--primary-text` + `--primary-border`, so an outline Cancel themes and competes with the confirm. Ghost is the neutral-slate carve-out that exists for exactly this, and it is what Dialog's and Drawer's stories already use. **Toast keeps `style="outline"`** (`ToastCard.tsx`, `size="xs"`) — owner's call: a ghost button on a floating `--popover` card reads as non-interactive. Don't "fix" ToastCard to ghost, and don't propagate Toast's outline back onto dialog footers. Pair the rule with the confirm's own variant: a destructive confirm stays `variant="error"` and never renders as the themed primary.

- **Vanilla CSS + React only.** No Tailwind. No Radix. No cva. No Framer Motion. No floating-ui. No CSS-in-JS. If the user's asked for a feature that would need one of these, offer them the trade-off explicitly (like Tooltip exit-animation interruption) and let them decide — don't silently reach for the dep.
- **BEM `ui-*` prefix.** Not per-component (`.button-*`), not per-product (`.foo-button`), not CSS Modules. This was renamed once already; don't reintroduce old names.
- **Product brand codes are 2 letters.** `db`, `dc`, `dr`, `ec`, `ir`, `nb`, `ph`, `rm`. Do not expand these codes to full product names — those were replaced during the codebase rename.
- **Package name is `@ui/lib`.** Don't propose different names without asking.
- **Every form control's built-in label uses `<Label>`.** Consumers can override by wrapping in a custom Label externally, but the component's internal label always goes through the shared class names.
- **Compound components use `Header/Body/Footer` naming** (Dialog, Card), not `Header/Content/Footer`. Internal consistency across our library is what matters.
- **Positioning has no collision detection.** Tooltip and DropdownMenu place popups at exact positions. If off-screen, they render off-screen. Adding flip logic would need `@floating-ui/react` — the user has declined.
- **Exit animations only exist on Tooltip.** Other portals unmount instantly on close. Adding exit animations elsewhere requires the interruption-jump state-machine pattern documented in `Tooltip.tsx`.

---

## When something's ambiguous

Ask the user before doing it. Don't guess at:
- Whether a new prop should be added to an existing component (adds API surface)
- What a variant should be named (naming is hard, they usually have opinions)
- Whether to introduce a new token vs. reuse an existing one
- Whether to break out a shared primitive (extract-vs-inline)

Small clarifying questions cost less than reworking a component after the fact.

---

## Roadmap — where we're headed next

**The Claude Design push shipped 2026-07-19 against the then-42-component roster, and was brought back to full coverage 2026-07-31: all 59 components have a preview card.** The six that had fallen behind (`Chat`, `Direction`, `Fab`, `FeaturedIcon`, `InputOTP`, `Menubar`) were backfilled, and `Chat` + `Fab` opened a **ninth group, `AI`** — the Aiden surface had no home in the original eight. **Immediate next up: generate a UI via Claude Design that actually uses these components, and iterate wherever the AI picks the wrong one** — that's the real test of whether the preview cards communicate what each component is for, and it is now an honest test.

### Prep

1. ~~`npm run build` — verify the library bundles cleanly end-to-end~~ **Done 2026-07-18.** Builds clean; `tsc --noEmit` clean; grep guardrails clean.
2. ~~Top-level `styles.css` bundle so consumers can import in one line~~ **Done** — `dist/styles.css` via the `@ui/lib/styles.css` export. It grows with the roster; at 59 components it is 173 kB / 23.5 kB gzip (was 121 kB / 17 kB at 42).

**Fonts are a separate opt-in import (2026-07-18) — don't re-couple them.** `tokens.scss` no longer `@import`s `./fonts`. Reason: Vite **forces asset inlining in library mode** (`build.assetsInlineLimit` is ignored there), so the 25 subsetted woff2 files were base64-inlined into `styles.css` — 685 kB / 438 kB gzip, and it *defeated the `unicode-range` subsetting*, making every consumer download Cyrillic/Greek/Vietnamese up front as render-blocking CSS.

Fonts now build via a separate `npm run build:fonts` (plain `sass` CLI, outside Vite's lib pipeline) → `dist/fonts.css` + `dist/fonts/*.woff2`, exported as `@ui/lib/fonts.css`. All 25 `@font-face` rules keep their `unicode-range`, so faces load lazily per script. Verified: mono is fetched only when something actually renders in it.

- Consumers: `import '@ui/lib/styles.css'` (required) + `import '@ui/lib/fonts.css'` (optional self-hosted Inter/JetBrains Mono).
- Storybook loads `src/styles/fonts.scss` explicitly in `.storybook/preview.tsx`.
- If you ever re-add `@import './fonts'` to `tokens.scss`, the 685 kB regression comes straight back.

### The push (via the `DesignSync` tool + `/design-sync` skill) — **shipped 2026-07-19, now 6 components behind**

Project **`@ui/lib`** (`49609428-9a74-47c7-a777-b9f56bd43b81`), `type: PROJECT_TYPE_DESIGN_SYSTEM`. `preview/` holds **63 files — 60 component previews plus 3 Foundations pages** (motion, palettes, themes). **Synced 2026-07-31** — all 62 plus `dist/styles.css` uploaded and verified present. **`FullScreenDialog` (2026-08-05) is NOT yet synced** — its card exists locally at `preview/overlays/full-screen-dialog.html` but the project has not been re-pushed, and the stylesheet up there predates the component's classes.

**A full sync was required, not the six new cards alone, and the reason generalises.** The project had last been written on 2026-07-22, so its `dist/styles.css` predated every one of the six components — their classes did not exist in it. But shipping the current stylesheet on its own would have broken the 31 already-published cards still referencing the retired `--sz-small` vocabulary. The preview HTML and the stylesheet are one artifact: **whenever a token or class name changes, they go up together or the project is left worse than before.**

**`_ds_manifest.json` is compiled app-side from each file's first-line `<!-- @dsCard group="…" -->` marker — it does NOT update when you write files.** Immediately after this sync it still listed 40 cards, missing the six new ones *and* 13 that had been published for over a week. That is expected lag, not a failed upload: verify a card landed by reading the file back and checking the marker survived on line 1, not by reading the manifest.

The previews are **tracked in this repo** under `preview/{group}/{kebab-name}.html` and uploaded from there — local is the source of truth, the project is a published copy. They can drift (they did: `dist/styles.css` sat at the pre-fix 685 kB build for two days, and the Toast preview kept hand-rolled action buttons after Toast moved to composing `Button`). **When you change a component's rendered markup or class names, update its preview in the same commit.**

Two conventions worth knowing before you write another one:
- **Static pages can't rely on entrance animations.** Drawer animates in with `animation-fill-mode: none`, so after the keyframes finish the panel reverts to its off-screen resting transform. `drawer.html` pins `animation: none; transform: none;` inside its `.stage` so every card renders identically.
- **Viewport-anchored components need re-anchoring.** `Drawer` is `position: fixed` and `.ui-sidebar__container` likewise; both previews scope a `.stage { position: relative }` and override the child to `absolute` so the component sits inside its own frame instead of the page viewport. `dialog.html` established this pattern. **Note why it works:** `position: relative` does NOT create a containing block for a `fixed` child — only `transform` / `filter` / `perspective` / `contain` do. The pattern is sound because it re-declares the child as `absolute`, which a relative parent *does* contain. `fab.html` follows it.
- **Surface-scoped styling has to be staged deliberately.** `Fab` fills with `--primary` and only becomes the Aiden gradient under `data-surface='aiden'`. A card that just drops the component on the page renders the neutral fill and quietly teaches the wrong thing — `fab.html` shows all three contexts (default, inside `data-theme`, inside the surface) side by side. Check the rendered fill, don't assume the component's headline description.

Steps, for reference / re-running:

1. ~~Create a Claude Design project~~ **Done** — must be `type: PROJECT_TYPE_DESIGN_SYSTEM` (set at creation, immutable)
2. Build HTML preview files, one per component (or per family), each with a `<!-- @dsCard group="…" -->` marker at the top so it appears as a card in Claude Design's Design System pane
3. Suggested groupings for the `group=` attribute:
   - **Foundations** — Themes, palettes, motion tokens preview, ModeToggler
   - **Buttons** — Button, ButtonGroup, CloseButton
   - **Forms** — Input, Textarea, Select, NativeSelect, Combobox, Checkbox, RadioGroup, Switch, Slider, Toggle, ToggleGroup, Field, InputGroup
   - **Feedback** — Alert, Banner, Badge, Progress, Spinner, Skeleton, Toast, Empty
   - **Overlays** — Dialog, AlertDialog, Drawer, Popover, Tooltip, HoverCard, DropdownMenu, ContextMenu, Command
   - **Navigation** — Tabs, Breadcrumb, Pagination, Sidebar, Item (list rows)
   - **Layout** — Card, Accordion, Collapsible, ScrollArea, Attachment, Separator, AspectRatio, Blockquote, Code
   - **Identity** — Avatar, Chip, Label, Kbd, StatusDot, FeaturedIcon
   - **AI** — Chat, Fab (added 2026-07-31; the Aiden surface had no home in the original eight)

   (Verified 2026-07-20 against the then-53 roster with no omissions. The six components added since — `Chat`, `Direction`, `Fab`, `FeaturedIcon`, `InputOTP`, `Menubar` — fit the existing groups: AI/Aiden work has no group yet, so `Chat` and `Fab` want one; `InputOTP` → Forms, `Menubar` → Overlays, `FeaturedIcon` → Identity, `Direction` → Foundations.)
4. Call `DesignSync` in sequence: `list_projects` → `finalize_plan` → `write_files` → verify

### After the push

- Test generating a UI via Claude Design that uses these components
- Iterate any misalignment between preview cards and how AI picks components

### Non-blocking follow-ups

- ~~**Round out the roster**~~ **Done 2026-07-19 — Slider shipped, closing the roster at 43 at the time; it now stands at 59.** Every component is exported from `src/index.ts`, verified programmatically. Drawer fills the "sheet"/edge-panel role. (Claude Design previews are no longer 1:1 — see the push section above.)
- **Figma design-system push — COMPLETE (2026-07-27). Every component has a doc page (all 5 phases shipped), template v4 LOCKED.** One doc page per component in file `jzc2ME8xVmfX1V8OCt2HC2`; Button (`1 · Button`) is the reference implementation. Phases 1–4 (forms · selection/status · overlays) done; Phase 5 (ModeToggler, Direction, Sidebar, Chat) closed it out — Chat was the final component. **Before touching the Figma file: read `docs/figma-playbook.md` (the recipe) and `docs/figma-ledger.json` (per-component status + all node/variable IDs + ~90 lessons).** Do not redesign the template without the owner. Key locked patterns: horizontal frames (Overview · Spec · [Theming] · Examples·Docs·History), owner's side-by-side light/dark spec tables with labeled separators, composition-forward overview (no props table), theme-able fills bound to the Theme collection, the `flag/is-dark` aiden dark-gradient overlay (never aiden-light/dark variants), and the all-zeros design-check lint as ship gate. **Newer conventions worth knowing:** atom masters live NESTED inside the Overview frame (never loose on the canvas); a sub-part that has its own code component gets its own atom (don't hand-draw it); an assembled template whose list must be extendable (Sidebar menu, Chat transcript) is a plain FRAME built from atom instances (via `detachInstance()`), NOT a locked component — Figma has no arbitrary-children slot; provider/hook components with no visual atom (Direction) get a concept page with the middle frame as a behaviour demo and no component master.
- **npm publish workflow** so any repo can `npm install @ui/lib`
- **Accessibility audit — first pass done 2026-07-19.** Swept all 42 for focus rings, ARIA wiring, live regions, label association, accessible names, and reduced motion. Two real findings, both fixed: no global `prefers-reduced-motion` support, and Chip's missing focus ring. Verified correct and needing no change: Dialog (focus trap + Escape + **focus restored to trigger**, confirmed end-to-end in a browser), Drawer (same), Command (proper `aria-activedescendant` combobox), Alert/Toast (`role` alert-vs-status by variant), Progress/Spinner/Skeleton, `htmlFor` on all 8 form controls, and accessible names on all 200 buttons across the preview HTML. **Still outstanding:** real screen-reader passes (VoiceOver/NVDA — ARIA wiring being correct is not the same as it announcing well), colour contrast beyond the secondary-button data recorded above, Sidebar's sub-768px Drawer swap, and tab order across composite widgets in real page layouts.
- **Dark-mode visual sweep** — verify every component renders correctly with `data-mode="dark"`

### Recent decisions worth remembering

- **"States live in the Spec table, not as variants" was never the file's convention — it described Button (2026-08-26).** The ledger had generalised a note about Button into a system rule. A census of 33 component sets found **20 carry a state-like axis** (Input/Textarea/NativeSelect `Error`+`Disabled`, Checkbox, Switch, RadioGroup, Slider `Disabled`, Chip `Active`, Toggle `Pressed`, and every menu/tab item with `State`) and only 13 did not. **Before arguing from convention here, run the census** — `componentPropertyDefinitions` over the ledger's `setIds` is cheap.
  - **The real pattern:** form controls carry **`Disabled` only** — hover and focus stay painted in the Spec table. Menu and tab *items* carry a `State` that includes hover. **Almost nothing carries focus as a variant.**
  - **Both Button sets gained `Disabled`** (120 → 240 variants each) so a disabled button can be *placed in a mockup*, not just read off a spec table. Its Spec frame already documented all four states correctly — the gap was composability, not documentation.
  - **`Disabled` cannot be a boolean.** A Figma BOOLEAN property only drives `visible`; disabled is an opacity change (`&:disabled { opacity: var(--o-50) }`), so it has to be a variant axis. That is what doubles the set.
  - **This is Figma-only — no code changed**, so it gets no Storybook changelog entry. See the ledger's `stateAxisCensus` for the 11 sets still without one; `Fab`, `CloseButton` and `ModeToggler` are the next most likely to bite.

- **`Tabs` gained a `variant` axis, and its tablist moved off `--muted` (2026-08-26).** Three treatments — `default` · `line` · `browser` — all **presentation only**: identical semantics, keyboard behaviour and aria wiring.
  - **The background fix was a real defect, not a preference.** `.ui-tabs__list` was `--muted` with `--muted-foreground` triggers on it — **5.1:1**, the pairing CLAUDE.md's own rule forbids and the one the 2026-07-21 sweep moved five other surfaces off. **Tabs was simply missed in that sweep.** Now `--accent`: **6.92:1 light**, and **byte-identical in dark**, where `--accent` and `--muted` both resolve to slate-700. `['muted-foreground','accent']` was added to `scripts/contrast-check.mjs` (now 56 pairings). The `['muted-foreground','muted']` guard's comment claimed no component did this any more — **that claim was false when written**, and the comment now records why.
  - **The sliding indicator is reused, never rebuilt per variant.** `Tabs.tsx` writes its `width`/`height`/`transform` as **inline styles** from the active trigger's offset box, so CSS cannot reshape it — inline wins. Each variant therefore leaves the box alone and repaints through a **pseudo-element**, inheriting the slide for free. Don't "fix" this by branching the JS.
  - **One root class, descendant selectors — no part gained a prop.** Same mechanism as RadioGroup's `size`. `variant` lives only on the root.
  - **`default` emits NO modifier class.** `.ui-tabs--default` would be a class nothing selects — the dead-BEM noise the repo scans for. Same call as Mark's `motion="none"`. `data-variant` is always present and is the hook for targeting the default case.
  - **`browser` is horizontal-only** and resolves to `line` under `orientation="vertical"` rather than render a column of browser tabs. **Its panel becomes the surface** (`--card` + border + radius), so **nesting a `Card` in a browser panel stacks two panels** — the other variants leave the panel unstyled, where a `Card` is correct.
  - **`browser` looks like `TabBar` and that is now deliberate** — owner's call, made knowing it blurs the visual line. The Tabs-vs-TabBar rule was rewritten as purely behavioural (see the routing rules above). A `Tabs` trigger stays a `<button>`; it has no close affordance and no overflow scrolling.

- **`Table` exists in Figma ONLY — the file's first proposal page (2026-08-26).** There is no `src/components/Table/`, and none is planned in this pass; the owner asked for the Figma component and chose "Figma only for now". **Do not add `Table` to the component roster above** — that table documents code, and Table is not code. Full design spec: [`docs/superpowers/specs/2026-08-26-figma-table-design.md`](./docs/superpowers/specs/2026-08-26-figma-table-design.md); build record + IDs in the ledger under `components.Table` (`isProposal: true`).
  - **A proposal page must say so on the line a designer actually scans.** The Overview chip that normally reads `src/components/{Name}` is replaced by **`⚠ proposal — not in src/`**, and a `PROPOSAL — NOT BUILT IN CODE` labelled separator sits over a red callout above the component sets. Same rule as the Code page's highlighted mock-ups: a labelled decision, never an unlabelled mock. The index board reads **"67 components + 1 proposal"**, not 68 — bumping the count would have been the lie.
  - **Row-major with fixed-width cells is the whole design.** Cells sit in a row, rows stack, so every row is a real node that can carry a hover/selected fill and its bottom hairline. Column-major would align columns for free but leaves **no row node at all** — no hover, no selection, no divider. The price is that column width is a hand-kept convention Figma does not enforce, which is why the Spec frame states it before anything else.
  - **Cells are transparent; the ROW carries the surface fill.** An opaque cell paints over the row state and hover/selection silently stops working. This matches CSS, where a `<td>` has no background either.
  - **Density ships `sm`/`default`/`lg`.** The owner said compact/comfortable; that would reopen the second size vocabulary the 2026-07-30 hard break deleted. Same three rungs, existing names.
  - **Open, recorded, not decided: `selected + hover` has no token.** `selected` is `--primary-light`, `hover` is neutral `--accent`; stacked they are a `color-mix` nothing expresses. The Spec frame paints it with `--primary-soft` as the **proposed** mapping and says so — accepting it would make Table the second consumer of `--primary-soft`, which today only the filled `secondary` button uses. Needs an owner call before any code.
  - **In dark, `--secondary` and `--card` both resolve to `#1e293b`**, so the table header is the same colour as the body and is separated by its hairline alone (light is `#e2e8f0` on `#ffffff`). Accepted and stated on the page. It **cannot** move to `--muted` — header labels are `--muted-foreground`, and that pairing is 5.1:1, below AA. A stronger dark header needs a *new* token, not a swap.

- **`TabBar` shipped as component #62, built from a Figma frame — and the frame had two wrong bindings (2026-08-11).** The browser-style strip of open documents (the `.dc-tabstrip` the DART Central prototype had hand-rolled). Owner's calls: name `TabBar`, tabs closable with the X on hover + open, underline on **`--primary`** so the bar themes with its application, hairlines on the system **`--border`**.
  - **Two fills in the Figma frame were bound to the wrong variables, and both rendered correctly anyway** — which is exactly why nobody caught them. The open tab's background was bound to `--muted-foreground` while painting `#f1f5f9` (that is `--accent`), and the inactive label read the raw `slate/500` ramp rather than the semantic `--muted-foreground` that holds the same value in light. A wrong binding is invisible until the mode flips or the token moves; **check what a fill is BOUND to, not what it looks like.** Both corrected in code and in the file.
  - **The underline was `--category-indigo`, a fixed tag colour.** It looked right only because the screen it was drawn on is themed `db`, whose primary is a near-identical blue. Under `rm` it would have stayed indigo on a magenta application.
  - **A tab is a `div[role="tab"]`, not a `<button>`, and that is load-bearing.** A closable tab must contain the close `<button>`, and a button inside a button is invalid HTML that browsers silently un-nest. Keeping the tab a div makes the nesting legal AND keeps the close control a real button with its own accessible name and focus. The cost is that Enter/Space are handled by hand, guarded on `e.target === e.currentTarget` so a key pressed on the close button is not stolen.
  - **`onClose` must `stopPropagation`.** Without it, closing a *background* tab bubbles the click to the tab and switches to the tab on its way out — verified by real click, not synthetically.
  - **Activation is `manual`, inverting `Tabs`'s default.** Same prop name and vocabulary, opposite default: a `Tabs` panel is already on screen, a `TabBar` tab is a whole document, and arrow-to-activate would mount and tear down a dashboard per keypress.
  - **The close button is hidden with `opacity`, never `display: none`** — it stays in the tab order and focus reveals it. It therefore holds its 20px of layout at rest, which is why a 19-character label truncates at the 200px cap even when no X is showing. That stability is deliberate: the alternative is a label that reflows under the pointer.
  - Preview card + the Figma mirror of the *doc page* remain in the deferred follow-up batch with the Aiden suite.

- **The Aiden chat suite shipped — Claude-parity in five phases (2026-08-09).** The Chat family grew from 27 to 35 parts (`ChatMessageAction`, `ChatError`, `ChatDisclaimer`, `ChatModelPicker`, `ChatComposerMenu`, `ChatArtifact`+`Card`, `ChatLayoutAside`), plus `ChatMarkdown` on its own subpath and the three `Aiden*` integration surfaces. Decisions that bind future work:
  - **Hard rule 1 now has its ONE scoped exception** — see the amended rule. The gate is mechanical: `grep react-markdown dist/index.js` must stay empty.
  - **`Aiden*` names the integration layer** (Panel/FullScreen/Launcher — they self-apply `data-surface="aiden"`); conversation parts stay `Chat*`. The launcher's contract: **submitting navigates**; it never routes and never renders a reply.
  - **The panel is non-modal and `--z-40`** — below the floating surfaces at 50, because a Select opened inside it portals to body and must paint above. The plan drafted `--z-60`; that would have been a bug.
  - **`ChatComposerContext` gained `registerKeyInterceptor` + `inputRef`** (additive). An open `ChatComposerMenu` owns arrows/Enter/Escape through it; Enter-sends is suppressed while a menu is open.
  - **`ChatComposerMenu` reuses `Command`'s CLASSES, not its logic** — Command's active item is internal to its own keyboard model, so the menu owns filtering/selection and borrows `.ui-command__item`/`data-active` the way Pagination borrows `.ui-button`. **The menu-item styling copy count is still four.**
  - **The history sidebar is a RECIPE** (`AI/Aiden History Sidebar` story), not a `ChatHistory*` family — promote only if sub-apps copy-paste-drift it. DropdownMenu still has no destructive item variant; add it first if promoting.
  - **`ChatLayoutAside` switches `ChatLayout` to a grid via `:has()`** — the no-aside path is byte-identical, which is what makes it additive.
  - **`DialogHeader` gained an `actions` slot** — DialogHeader renders explicit JSX children, so spread children are SILENTLY DROPPED; the named slot exists because a `headerActions` prop passed as children went nowhere.
  - **`Badge` takes `label`, never children** — children are silently dropped (explicit-children override). This shipped empty pills in three places before being caught; grep for `<Badge` with JSX children when touching badge call sites.
  - **`--aiden-text` closed the last recorded aiden AA gap** (3.84 → 7.95 measured); the contrast checker now FAILS on a pairing that resolves in zero contexts instead of skipping it.
  - **Figma mirror + preview cards for all of this: the deferred follow-up batch.**

- **`Mark` shipped as component #61 — the application mark (2026-08-08).** The Figma/POC mark became a real library component: the brand's `--decorative-gradient` under a stack of glass, optionally locked up with the application's name. It is the only chrome that carries a brand's **full three-anchor identity** rather than just `--primary` — which is the payoff of deeper theming v2, and the reason a themed rail doesn't have to shout.
  - **No `brand` prop, deliberately.** The mark reads the `data-theme` / `data-surface` it stands in, exactly like every other themed component. A `brand` prop would be a second, competing way to select a theme and would drift from the page around it.
  - **`motion` gates animation and NOTHING else.** `none` / `ambient` / `tilt` all render identical colours. The POC's still mark was *not* mode-aware while its live one was, so turning motion off silently repainted the tile; they are one mark now. `none` emits **no modifier class** — it is the absence of the animation rules, and a class nothing selects is exactly the dead-BEM noise the library scans for.
  - **THE MAIN BRAND HAS NO `--decorative-*` AND MUST NOT GET ANY.** Those tokens exist only inside a brand scope, and `Chart.scss` depends on that: it reads `var(--decorative-hi, currentColor)` so an unthemed chart keeps a neutral marker. Defining `--decorative-hi` at `:root` to feed the mark would silently repaint every unthemed chart. The mark carries **`--mark-ramp`** instead — a neutral slate ramp on the same four-stop plateau geometry. **The fallback in `Mark.scss` is load-bearing:** an undefined `var()` in a comma-separated `background-image` does not drop that one layer, it invalidates the **whole property**, so the tile painted `none` and the near-white glyph vanished. That is how the unthemed mark first rendered — an empty white square, on the default no-theme page a consumer sees first.
  - **The dark glass is the OTHER HALF of the same lamp, not a darkened copy.** A specular highlight is by definition brighter than its surface, so on a pale dark-mode tile the white stack simply stops existing. The top-left bloom becomes a bottom-right occlusion and the sheen rises from the base instead of falling from the top. Only the spark survives white, because a small hard edge reads where a 24%-alpha wash does not.
  - **The sheen's pinned edge may only ever move one way, and the direction inverts by mode.** Its brightest (light) or darkest (dark) edge is anchored, so any move that lifts it exposes a hard line the full width of the tile. Light uses `(--my - 1)` (always ≤ 0), dark needs `(--my + 1)` (always ≥ 0), and `transform-origin` must sit on the anchored edge in both. The POC hit this **three separate times** — keyframes, tilt translate, dark geometry. Verified here at full deflection in both modes.
  - **The glyph is `--primary-foreground`, which is already mode-split.** A flat white glyph measured 1.74–2.66:1 on the pale dark tiles — under the 3:1 graphical floor for every brand. Measured after the change: worst 4.53 light / 6.44 dark.
  - `usePointerTilt` was promoted from the prototype to `src/hooks/` and is exported.

- **Deeper theming v2 shipped into `tokens.scss` — Phase A (2026-08-08).** The POC became the real token system. Full record, written for someone who wasn't here, is **`docs/deeper-theming-v2-merge.md`** — read it before touching the theming layer. Headlines:
  - **`dr` and `ir` are retired.** They had no solved anchors, so shipping them meant a scope where `--primary` swaps but surfaces, artwork and charts don't — a split system, worse than either half. Reversible with the same machinery. Six brands ship.
  - **Every brand's primary moved to its solved value. `rm` went violet → magenta and `nb` teal-green → grass.** Any screenshot, marketing asset or printed swatch of those two is out of date.
  - **`--chart-1..6` silently stop being neutral slate inside a theme scope.** No token renamed, nothing errors — a consumer expecting the slate ramp under a brand now gets brand colour. Intended.
  - **`--category-indigo-text` and `--category-violet-text` (dark) are the only two exceptions to "dark `-text` reuses the base hue."** Tinting the dark page lightens it, costing ~0.6 contrast; indigo carried only 0.29 of headroom and fell to 4.18 under *every* brand. Both took their palette's 300 step. Don't darken them back.
  - **Guardrails were built BEFORE the tokens moved, and that ordering is the point.** `test:contrast` went from 2 contexts to 52 (mode × theme × tint) and immediately found three real defects the old one structurally could not see — including one pairing that had never been checked at all, because `--primary: var(--primary-main)` defeated its parser and an unresolvable value was **skipped**, which is indistinguishable from passing. If you extend a guardrail, make an unmatched/unresolvable case a FINDING, never a silent `continue`.
  - **Open, recorded, not fixed:** `rm` dark has an adjacent chart-slot pair 6.3 apart under protanopia (below the repo's own floor of 8). It's listed in `CVD_RECORDED` in `check-palette.mjs`, so the run passes and prints it under "RECORDED, NOT FIXED" every time. Deferred to the charting rebuild along with `rm` light's 5.1 muted separation and `ec` dark's slot-1 exception.
  - **Page surfaces inside the rail follow the RAIL, not the page.** `.ui-sidebar__input` and the outline menu button paint with `--background`, tinted by `--tint-page` — an independent switch from `--tint-rail` — so `data-tint="rail"` alone moved the rail and left its own contents behind (field/rail gap 1.22 → **1.45:1** in dark; the search box read as a hole punched in a lighter rail). `--background` is now re-derived on `.ui-sidebar__inner` with the rail's own percentages, holding the relationship across all four tint states. **Scoped to `__inner` deliberately — `.ui-sidebar__inset` is the real content area and must keep the true page surface.** Generalises: any region tinted by one multiplier that contains surfaces driven by another will diverge on the single-axis states, and `page rail` hides it by moving both.
  - **Phase B is not started:** chart 7th slot, `--chart-seq/div-*` and the `[data-chart-palette]` axis, Card shadow stack, POC teardown.

- **`background-image` cannot interpolate from `none` (2026-08-08).** Sidebar hover was fixed by layering an `--opacity-50` gradient over `--sidebar-accent`. It looked correct and broke the animation: the base is `background: transparent` with `transition: background`, so hover used to fade colour → colour, and a gradient turned every row into a snap. Any hover/active state on a transitioning `background` must stay a **colour** — `color-mix` is the tool, not a gradient overlay. Same trap anywhere a transitioned background gains an image.

- **`.ui-docs-stage` carries `contain: layout` (2026-08-08).** `position: relative` does NOT create a containing block for a `position: fixed` child — only `transform`, `filter`, `perspective` and `contain` do. Without it every viewport-fixed component escaped its docs card and pinned to the iframe: the Sidebar page rendered **five** sidebars stacked on the window edges over the prose. `contain: layout` is the cheapest of the four (no paint or size boundary, so a legitimately-overflowing story still behaves). Covers Dialog, Drawer, Toast and Fab too. Same root cause as the Fab stories note below.

- **`FullScreenDialog` shipped as component #60, and three Dialog fixes came with it (2026-08-05).** The page-over-a-page surface — Help / Feedback / settings opened from a workspace sidebar. Owner's calls: edge-to-edge with **no rounding**, Dialog's **centre-scale** motion, **X + Escape only** (no click-outside, so a stray click can't discard a form), and a **composable** header.
  - **Built as a preset of `Dialog`, and that is the whole design.** No new portal, focus trap, scroll lock, focus restore, aria wiring or exit machine; Header/Body/Footer are Dialog's chrome, not a third copy. Only geometry and Escape are new. Because the panel covers the viewport and the motion is Dialog's, **zero new keyframes** were needed.
  - **`width/height: 100%`, never `100vh`/`100dvh`.** `.ui-dialog-overlay` is already `position: fixed; inset: 0`, so its containing block IS the viewport and its height is definite. A fixed box also tracks the **small** viewport on iOS, so this behaves like `100svh` and does not jump with the URL bar — `dvh` would be *worse*. Fragile dependency: if `.ui-dialog-overlay` ever becomes min-height-based, `height: 100%` silently collapses to zero.
  - **Every override selector is TWO classes (`.ui-dialog.ui-full-screen-dialog`).** Single-class overrides tie `.ui-dialog` at (0,1,0) and resolve on source order. **Null `max-width` AND `max-height` explicitly** — a `width` alone is only a *preferred* width and stays clamped by the inherited `max-width` no matter which stylesheet wins. **`.ui-command-dialog` shipped that bug** (2026-07-29 → fixed 2026-08-05): it set `width: min(560px, …)` and never declared a `max-width`, so the palette rendered **448px, not 560px** — measured in the browser, before and after. Worth noting the *mechanism*, because the first diagnosis got it wrong: Command.scss loads **after** Dialog.scss and won every tie it actually contested (its `max-height: 560px` beat `85vh`, its `__header { display: none }` beat `display: flex`). Nothing lost on source order — the declaration was simply **absent**. Two classes are still the right shape, as insurance rather than as the fix.
  - **The centred content column is a grid track, not a wrapper div.** `grid-template-columns: min(100%, var(--ui-fsd-content-width))` + `justify-content: center` on `.ui-dialog__body`. This keeps the body as the **full-width scroll container**, so the scrollbar hugs the viewport rather than the column edge — capping the body's own width is what makes a full-screen page look broken. `min()` not `minmax()`: a `minmax` with a fixed max sizes the track to that length and overflows a narrow viewport. Side effect to know: the body is now a grid, so `DialogBody`'s `alignment` prop no longer centres the block there, only its text.
  - **Composable header + no click-outside = a possible inescapable overlay**, so safety is by construction, not by warning (there is still **zero `console.warn` in `src/`** — don't introduce the first one). `FullScreenDialogHeader` takes `id`/`onClose` from context so `<FullScreenDialogHeader title="…" />` is the whole correct usage, `showCloseButton` defaults on, and `title` is required (it seeds the id Dialog's conditional `aria-labelledby` points at).
  - **Escape is handled by the component, not by Dialog** — hence the new additive **`Dialog.closeOnEscape`** (default `true`, no behaviour change). `Select`/`Combobox`/`DropdownMenu`/`Popover` all register **bubble-phase** `document` keydown with **no `stopPropagation`** (verified: only ContextMenu uses capture, only Combobox stops), so one Escape closes the menu *and* the modal behind it. FullScreenDialog returns early when an open surface is in the DOM. **This is a workaround in the wrong place** — `Dialog` and `Drawer` still have the bug, and the selector list is a maintenance liability. The real fix (capture + `stopPropagation` in those four) is its own change.
  - **Three Dialog fixes landed with it.** (1) The **scroll lock is now its own effect keyed on a mounted boolean**, mirroring Drawer, so it is held through the exit instead of released at the *start* of `closing`. Note the symptom was **not** a page shift — the `padding-right` swap is symmetric — it was that `previouslyFocused.focus()` in the same cleanup scrolls its target into view, so restoring focus to an off-screen trigger scroll-jumped the page mid-exit. The dep **must** be the boolean; keyed on `state` the effect re-runs on `open → closing` and flashes the scrollbar. (2) `onAnimationEnd` now checks **`e.target === e.currentTarget`** — animation events bubble, and an `inline` Dialog nested inside carries the same `ui-overlay-exit--center` class, so its `ui-overlay-out-center` was unmounting the outer one. (3) `closeOnEscape`. Verified Dialog, AlertDialog and CommandDialog all still open/lock/trap/restore correctly.
  - **`Fab` (`--z-80`) floats ON TOP of this** (`--z-50`), breaking the edge-to-edge contract. Left alone deliberately — changing `Fab.scss` would also change its relationship to normal dialogs. Consumers unmount the Fab while a FullScreenDialog is open. `Toast` (`--z-80`) and `Tooltip` (`--z-70`) sitting above are correct and wanted.

- **Bespoke Storybook Docs template (2026-07-28):** stock autodocs was replaced with our own page, registered globally as `parameters.docs.page` in `.storybook/preview.tsx`. Shape follows the conventional component-doc reading order — header · Preview/Code · Install · Usage · Composition · Examples · API reference · Accessibility — but every part is built from the library's own components (`Tabs`, `Badge`, `CodeBlock`, `Item`/`ItemGroup`, `Kbd`) and tokens, so the docs page is itself a proof the system composes. Lives in **`.storybook/docs/`**, deliberately outside `src/` — verified the shipped bundles (`dist/index.js`, `dist/index.cjs`, `dist/styles.css`) contain zero Storybook references. One `.d.ts` does: `dist/types/DocsTypes.d.ts` carries a `.storybook/docs/DocsPage.tsx` path in a doc comment. `DocsTypes` is types-only, isn't exported from `src/index.ts`, and is erased at build — it costs consumers nothing, but the claim is "no Storybook in the runtime bundle", not "no Storybook anywhere in `dist/`".
  - **Prose lives in `meta.parameters.ui`**, typed by `src/types/DocsTypes.ts` (`UiDocsParameters`: `description`, `status`, `tags`, `usage.when`/`.avoid`/`.notes`, `composition[]`, `a11y.keyboard`/`.notes`, `importCode`). **All strings, no ReactNode** — parameters cross Storybook's channel on every story-prepared message. `DocsTypes.ts` is types-only and is **not** exported from `src/index.ts`. Every section renders only when its data exists, so a component with no `parameters.ui` still gets a complete-looking page (Kbd verified). **All 59 components now carry a `description` + `tags`;** Button and Select additionally carry `usage` / `a11y`, and Select carries `composition` + the repo's only `subcomponents` declaration (which drives its per-part props tables). Those two are the model to follow when filling in the rest.
  - **`parameters.ui` prose supports exactly four Markdown affordances: `` `code` ``, `{@link Name}`, `**bold**`, and a blank line for a paragraph break (2026-08-05).** The first three are parsed by `kit.tsx`'s `prose()` at render time; the fourth is `white-space: pre-line` on the three elements that render prose (`.ui-docs__lede`, `.ui-docs-p`, `.ui-docs-example__desc`). Anything else renders literally — no headings, lists or links; a docs string that needs them wants to be a story. **Every prose field must go through `prose()`**, or the markup shows up literally: lede, usage bullets + notes, a11y notes, composition part descriptions, keyboard row descriptions, per-story `docs.description.story`, and the props-table description column (JSDoc is full of them). Nine call sites in `DocsPage.tsx` + `PropsTable.tsx`.
    - Bold and paragraph breaks were added after ten story files were found authoring both against a template that rendered neither — literal `**asterisks**` on the page, and two paragraphs run together as one block. The alternative (strip the markup, keep backticks as the only affordance) was considered and rejected: the authors' intent was unambiguous and the rule would have kept getting violated.
    - **`prose()` applies Markdown's paragraph rule before anything else — a single newline is a source wrap, a blank line is a break (`unwrap()`).** This is load-bearing, not tidying. Prose arrives from two places that wrap differently: `parameters.ui` strings (concatenated literals, no incidental newlines) and **JSDoc comments above a story export or a prop, which reach docgen with the author's source wrapping intact**. Without the unwrap, `pre-line` breaks those JSDoc-authored descriptions mid-sentence at whatever column the comment happened to wrap at — it did, on the seven component stories documented that way (Avatar ×2, Card, Chip, CloseButton, Empty, FullScreenDialog). Caught only by reading rendered `textContent`, since the source looks fine.
    - **Do NOT add `pre-line` to `.ui-docs-props__desc`.** Props descriptions are JSDoc, and the unwrap makes them single-paragraph by design; a `\n\n` there would be an authoring accident, not a break.
    - Prototype/showcase pages (`!preparedMeta.component`) render the meta lede and the stories, but **not** per-story descriptions — a JSDoc block above a prototype story export is dead text on the page.
  - **Header type: the lede is `--text-lg` on `--leading-7`.** It was `--leading-8` (a 1.78 ratio — body-copy leading on lede-sized type), which pulled the lines apart and detached the paragraph from the title; the gap under the title came down to `--p-3` once the block tightened.
  - **The `propFilter` in `main.ts` was wrong and silently hid props.** Filtering on `prop.parent` dropped every key we *redeclare* — docgen reports `parent` as the `@types/react` interface it saw first, so Button lost `id`, `className` and `type`. Now filters on **`prop.declarations`** ("declared anywhere outside node_modules"): Button 10 → 15 props. Don't revert it.
  - **`docs.source.type: 'dynamic'`** is set globally. 93% of our stories use a custom `render`, and Storybook's default `auto` prints the raw CSF export for those — readers got `{ parameters: {...}, render: () => ... }` instead of JSX.
  - **Every example is ONE card — demo over its own source (`kit.tsx` `Block` + `CodePane.tsx`).** There is no Preview/Code tab pair; the same block serves the page's primary preview and every example below it, so the page reads as one repeating unit. `.ui-docs-block` owns the border/radius/shadow; `.ui-docs-stage` and `.ui-docs-code` carry none of their own (the code strip is separated by a single `border-top` hairline). Two separate framed boxes with a gap between them read as unrelated panels — don't split them again.
  - **Code strips collapse.** Printing every story's source in full made Button's page 17,300px tall. Collapsed shows three lines (`--h-28`) under a full-height fade with a **"View Code"** pill floating dead-centre over it; expanded caps at `--h-96`, scrolls *inside itself*, and the pill moves to `bottom` and reads "Collapse". Button 17,313 → 8,094px; Chat, the worst case, 17,977 → 10,233px; median page 3,480px. Expanded state adds `padding-bottom: var(--p-16)` to the `pre` **and** the gutter so the floating pill never sits on the last lines.
  - **The pane owns the code surface.** `docs.scss` sets Storybook's `.docblock-source` to `background: transparent` and paints `--card` on `.ui-docs-code`, which is what lets the fade gradient end on exactly the right colour without hardcoding Storybook's `#1b1c1d` — and puts the code on the same surface as the stage above it. Don't give `.docblock-source` its background back. The type ramp is pinned too (`--font-family-mono` / `--text-code` / `--leading-5`); Storybook's default is bare `monospace` at an inherited line-height, which drifts per platform.
  - **Never put a positioning `transform` on a `Button` — put it on a wrapper.** The "View Code" pill was centred with `transform: translate(-50%, -50%)` *on the Button itself*. `.ui-button:active:not(:disabled):not(.ui-button--loading)` sets `transform: scale(var(--motion-scale-press))` at specificity (0,4,0) — `transform` **replaces**, it doesn't compose — so on mousedown the button teleported half its own size down and right, out from under the cursor; mouseup landed on something else and **no `click` event ever fired**. Symptom: "the button moves a lot and nothing happens." Applies to any component with a press-scale (Button, Chip, Toggle, ToggleGroupItem). **This also caught out the verification:** a synthetic `pointerdown→mouseup→click` dispatch passed, because dispatching `click` explicitly bypasses the browser's own "was mouseup over the same element" rule, and synthetic events don't trigger CSS `:active` at all. Only a *trusted* click reproduces it. When testing a floating/overlaid control through the Browser pane, note that `computer` coordinates are **not** 1:1 with page pixels (measured ×2.57 here) — calibrate by recording `e.clientX/Y` from a real click before trusting a hit.
  - **The Motion section is DERIVED, not authored (2026-07-30).** Every component doc page that animates gets a Motion section listing its duration / easing / distance tokens with resolved values, its `@keyframes`, and any motion it inherits. `.storybook/docs/motion.ts` reads the SCSS through `import.meta.glob(..., '?raw')` at render time — hand-copying ~40 token lists into `parameters.ui` would have started accurate and quietly stopped being so. `parameters.ui.motion` carries only what a machine cannot infer: prose and named `moments` (trigger → what happens). Three things it got wrong first, all worth knowing: **(1) strip comments before parsing** — `tokens.scss` documents `--ease-premium` in prose that names it, so the value regex matched inside the comment and rendered a paragraph instead of a cubic-bezier; Drawer.scss's comment about `@keyframes` produced a phantom keyframe called `parks`. **(2) `displayName` is not the directory** — Toast's is `Toaster`, Direction's is `DirectionProvider`, so a displayName lookup silently returned nothing and the section just didn't render; resolve by the story title's last segment first. **(3) Detect inherited motion via SCSS imports, not class names** — Textarea/NativeSelect import Input's stylesheet, Pagination imports Button's, ToggleGroup imports Toggle's, CloseButton imports `icon-button.scss`. Sniffing for class names missed all of them; reading the `import '…scss'` statements catches every one.
  - **`.storybook/` is typechecked, and the include pattern is load-bearing (2026-07-30).** It previously was not — `"include": ["src"]` meant the whole docs template never saw `tsc`, and a bad prop (`<P className>` on a primitive that takes none) shipped silently. **Adding `".storybook"` to `include` does NOT work: TypeScript's wildcard matching skips directories beginning with a dot, so the entry matches nothing and `tsc` still reports zero errors — a green check that proves nothing.** It must be `".storybook/**/*"`. Verified the only honest way: inject a deliberate type error into each of `motion.ts`, `kit.tsx`, `preview.tsx` and `main.ts` and confirm `tsc` catches it. The bare-directory form caught none of them. Fixing the include surfaced one real pre-existing error — `useOf('meta')` returns a union across component/meta/story and does not narrow on its literal argument, so destructuring `csfFile`/`preparedMeta` straight off it was invalid; now narrowed with an explicit guard rather than a cast. **`npm run build` runs `tsc --noEmit` first, so the docs template is now a build gate.**
  - **A flex item defaults to `min-width: auto` and will not shrink below its content — this bit the docs stage (2026-07-30).** `.ui-docs-stage__inner` centres each story in a flex row, so Storybook's `.sb-story` wrapper is the flex item. With the default `min-width: auto` a story wider than the stage refused to shrink, overflowed its card, and — worse — any `max-width: 100%` *inside* the story then resolved against the overflowing width, so it could never claw itself back. `> .sb-story { min-width: 0; max-width: 100% }` is the fix. Symptom to recognise: a story that renders at its natural width no matter how narrow the card gets.
  - **`position: relative` does NOT contain a `position: fixed` child** — only `transform`, `filter`, `perspective` and `contain` create a containing block for fixed descendants. Fab's stories wrapped the FAB in `position: relative` frames and every one of them escaped to the iframe viewport and piled up in the corner, leaving five example cards looking empty. Any story previewing a viewport-fixed component needs `contain: layout` (or a transform) on its frame. Same class of problem the `preview/` HTML solved with its `.stage { position: relative }` + child-to-`absolute` convention.
  - **Line numbers are rendered by `CodePane`, not CSS.** `<Source>` emits a single `<pre>` of highlighted text with no per-line elements, so a counter has nothing to count. The block is `white-space: pre` on a fixed leading, so CodePane counts newlines (via a `MutationObserver`, since `<Source>` resolves its snippet asynchronously and re-renders on a mode flip) and stacks numbers at the same `--leading-5`. Alignment depends on the gutter's vertical padding matching the `pre`'s. The viewport is a flex row — gutter fixed, `.docblock-source` `flex: 1; overflow-x: auto` — so horizontal scrolling never slides the numbers out of view.
  - **`<Source>` and `<Story>` come from `@storybook/blocks`** (now an explicit devDep, previously only a hoisted transitive of addon-essentials). Source is syntax-highlighted; our `CodeBlock` isn't (the no-dep trade-off) so it takes the hand-written snippets. Storybook themes Source through emotion, which knows nothing about `data-mode` — left alone it renders near-black text on our dark page. **`.storybook/docs/useDocsMode.ts`** watches the `data-mode` attribute with a MutationObserver and feeds `<Source dark={…} />`.
  - **Foundations / Hooks / Prototypes get a stripped "showcase" page** — header + each story full-bleed + a TOC. Keyed off `!preparedMeta.component` (none of those 23 story files declare one), so zero story-file edits. Framing them with "Install / API reference" would be actively wrong — there is no `import { Themes } from '@ui/lib'`.
  - **`preview.scss` now styles `#storybook-docs`, not just `#storybook-root`** — that omission meant every Docs page ignored `--background` and stayed white in dark mode. The `.sbdocs-wrapper` overrides in `docs.scss` need the `#storybook-docs` id prefix to outrank Storybook's emotion theme.
  - **The TOC rail deliberately does not use an IntersectionObserver.** Sections here are tall and adjacent so several intersect at once; an observer biased to a top band always highlighted the section that was *leaving* it, one behind the heading on screen. It's a rAF-throttled scroll listener picking "the last section whose top crossed 96px", with an at-bottom special case (a short final section can never reach the marker).
  - **Every component page ends in a Changelog, and you MUST add to it (2026-07-29).** `parameters.ui.changelog` is a `UiDocsChange[]`, **newest first**, rendered as the last section on every component doc page. **Any change to a component adds an entry in the same commit** — the docs page is where a consumer finds out whether something moved under them. Each entry is three fields: `date` (ISO `YYYY-MM-DD`), `summary` (plain language, for someone who *uses* the component — no file paths), and optional `detail` (the prop, token or mechanism, for someone who *maintains* it). **There is no author field, by design** — git already records who, and names on a docs page invite reading changes as personal rather than systemic. All 59 currently carry a single "Initial build complete" entry dated 2026-07-29; that is the baseline, not a placeholder to overwrite.

  - **Every prop has a description (wave 5, 2026-07-29).** 548 of 849 carry hand-written JSDoc on their `{Name}.types.ts` surface; the other 301 come from a **standing-copy map in `PropsTable.tsx`** for the eleven props that mean the same thing everywhere (`className`, `id`, `rel`, `disabled`, `children`, the three `on*Change` callbacks, and the native `placeholder` / `required` / `type` that stories surface via `args`). **Source text always wins** — the map is only a fallback, so a component that seeds specific child ids still says so. Don't write those eleven out per-component: that was 301 copies of one sentence with 301 chances to drift.
  - **`{@link X}` and backticks in JSDoc are rendered, not printed.** `kit.tsx`'s `prose()` turns both into inline `Code`. `{@link}` is a real cross-reference in an editor but shows up as literal braces in a table, and there are 45 of them.
  - **Editing `{Name}.types.ts` does NOT refresh the props table.** Vite runs docgen on `{Name}.tsx`, and a change to the types file doesn't invalidate that module — the table keeps serving stale descriptions until Storybook restarts. Nine components looked undocumented for a while purely because of this; restart before concluding JSDoc "didn't work".
  - **Known follow-up:** docgen reports `id` as *optional* on any component whose props are `Omit<React.*HTMLAttributes<T>, …> & { id: string }` — TS resolves the intersection as required, but docgen picks the optional declaration. Adding `'id'` to each `Omit` list is a behaviour-neutral one-line fix per component; it's a ~50-file sweep, not done here. `Select` reads correctly already because `SelectProps` isn't an intersection.

- **No dead BEM modifiers — keep the scan at zero (2026-07-28).** Six classes were emitted by a `.tsx` and styled by nothing: `ui-input-wrap--has-left/--has-right`, `ui-input-group--disabled/--error`, `ui-chat-bubble--pending`, `ui-fab--pulse`. In every case the real styling hangs off a **child element** (`__icon--left`, `__typing`, `__rings`) or off a `data-*` attribute on the same element, so the modifier was pure noise. Also removed `.ui-dialog__{header,footer}--sticky`, dead in both directions — `.ui-dialog` is a flex column where only `__body` scrolls and header/footer are `flex-shrink: 0` *outside* that scroll container, so `position: sticky` on them could never have applied. **Check with: every `ui-x--mod` literal in a `.tsx` must exist in a `.scss`.** The reverse direction is not checkable — modifiers are built by interpolation (`ui-avatar--sz-${size}`), so scanning for styled-but-unemitted returns ~280 false positives. When you delete a class, grep `preview/` too — the preview HTML is hand-written and won't fail a build.

- **Size vocabulary: ONE scale — `xs`/`sm`/`default`/`lg` (2026-07-30).** Supersedes the 2026-07-28 alias decision below. The split is gone: Button and Chip now use the abbreviated scale like the other 26 size-bearing components, **and the emitted class changed with the prop** (`ui-button--sz-sm`). `normalizeSize` and `CanonicalSize` are **deleted**; `Size` is now derived from a runtime `SIZES` const in `types/GlobalTypes.ts`, and `ChipSize` is `Exclude<Size, 'lg'>`. **This is a hard break** — `xsmall`/`small`/`large` no longer compile, and hand-written CSS targeting the old class names breaks. That was the point: `tsc` then enumerates every typed call site, and the two places it *can't* see (three raw `ui-button--sz-small` strings in `Pagination.tsx`, and `Chat.tsx`'s `size = 'small'` default parameter) are exactly why the alias approach kept leaking. **Storybook `argTypes.options` now derives from `SIZES`** so the control list and the union cannot drift — a value missing from `options` is silently rejected and falls back to the default arg. `npm run test:preview` gates both the retired vocabulary and preview-HTML class drift. **Still out of scope:** `InputGroupButton`'s `icon-xs`/`icon-sm`, which smuggle a *shape* axis into the size prop — it is now the last place in the library expressing icon-only as a size. Chip still has no `lg` rung, deliberately. Figma's variant options are still title-case spelled-out; renaming them across Button's 240 variants is a named follow-up.
- **Size vocabulary: Button + Chip accept BOTH spellings (2026-07-28) — SUPERSEDED 2026-07-30, see above.** 23 components use the abbreviated scale (`sm`/`default`/`lg`, with `xs` on Attachment/Item/InputGroupButton); only **Button** (`xsmall|small|default|large`) and **Chip** (`xsmall|small|default`) were written spelled-out. Owner chose to **alias, not rename** — both now accept `xs`/`sm`/`lg` too, and **`normalizeSize` (`src/utils/size.ts`) maps them to the canonical spelling *before the class name is built***, so `ui-button--sz-small` is byte-identical whichever you pass. That is load-bearing: the preview HTML and the Figma mapping key off those exact strings, and the "don't mass-rename BEM" decision still holds. Widening a union is backward compatible, so no consumer breaks. **Prefer the abbreviations in new code**; the spelled-out forms are back-compat only and can go at a major. `Size` (widened) and the new `CanonicalSize` are both exported. Storybook `argTypes.options` must list both spellings or Storybook silently rejects an alias and falls back to the arg default — that cost a false negative during verification.

- **Every hard-coded `aria-label` is overridable (2026-07-28).** Two patterns, both valid: put the default **before** `{...rest}` (Spinner is the reference — ModeToggler, Pagination prev/next, Command root, Sidebar ×2, Chat's version pager all follow it), or expose a **prop** when the label sits on an internal element `...rest` can't reach (`Toaster.label`/`.dismissLabel`, `ChatBubble.typingLabel`, `ChatMessageEdit.editLabel`, `Combobox.clearLabel`). Controlled attributes that are *derived state* — `role`, `aria-checked`, `aria-expanded` — deliberately stay **after** the spread. Keep it at zero: a scan for `aria-label="…"` with no later `{...rest}` on the same element should come back empty.

- **`aria-hidden` belongs on the icon, never the wrapper (2026-07-28).** Breadcrumb's and Pagination's ellipsis both pair a decorative glyph with sr-only text. Putting `aria-hidden` on the enclosing element removes the *whole subtree* from the accessibility tree, sr-only text included, so the element announces nothing at all. Hide the `<svg>` only.

- **Component audit + the interaction blind spot (2026-07-28):** a full 59-component audit (`docs/component-audit-2026-07-28.md`, scored 78/100) drove four fix waves on branch `audit-fixes`: accessible-name pollution from `Label.description` (it now renders `aria-hidden` with an id the control references via `aria-describedby` — repaired Input/NativeSelect/Textarea/Checkbox/Switch/RadioGroup/Slider at once), five components that accepted no `{...rest}` (Dialog, AlertDialog, Tooltip, Spinner, InputOTP) plus Alert's `style` prop colliding with native `style`, 16 verified functional bugs, and an APG/WCAG pattern wave (Chat `role="log"` + tab stop, Toast pause-on-hover, ContextMenu Shift+F10, single `role="combobox"`, conditional `aria-labelledby`). **The lasting lesson is what the audit MISSED:** it was static-only — four reviewers read props, types, SCSS and ARIA but never *drove* a component. Two real bugs surfaced within hours, both pure interaction geometry: (1) Select/Combobox's chevron was a flex **sibling** of the trigger, not an overlay, so its `pointer-events: none` dropped clicks into a ~37px dead strip (the wrap had `cursor: pointer`, so it read as "broken", not "small target"); (2) every floating surface except Tooltip never re-measured, so any layout shift stranded the menu away from its trigger. **Neither is visible in source** — one needs measured rendered boxes, the other is an *absence* of a listener. Any future audit must add: a hit-target pass (click target must cover the full visual affordance), a live-interaction pass using real `pointerdown→mousedown→mouseup→click` sequences (**synthetic `.click()` lies** — it reported Select as working when a real click didn't), a layout-change pass (resize/scroll with each overlay open), and explicit absence checks against a per-pattern checklist.

- **`size` added to Checkbox + RadioGroup (2026-07-23):** these were the two form controls missing a `size` (Input/Switch/Slider/NativeSelect/Toggle/Chip all had one) — an inconsistency, not a deliberate carve-out, so it was closed. Both use `sm/default/lg`. **Checkbox** puts `ui-checkbox-wrap--sz-{size}` on the wrap; sm/lg override the box (`--w-3-5`/`--w-5`) + check svg, and each size sets `.ui-label__text` font-size (default is now *explicit* `--text-sm`, previously inherited — matches Input/Label). Base `.ui-checkbox` still supplies the default box + the shared `margin-top: --p-0-5`, which aligns the box against the first text line at all three sizes. **RadioGroup** puts `ui-radio-group--sz-{size}` on the group root and scales everything from there — `.ui-radio` circle, `.ui-radio__dot`, and all `.ui-label__text` (group header + item labels) — **no context/prop threading to items needed** (one root class, descendant selectors). Verified all three sizes render + align in Storybook.

- **Menubar shipped — reuses DropdownMenu wholesale (2026-07-23):** the desktop-style menu bar, off the deferred roster. **Did NOT add a 5th menu-item styling copy** — instead each `MenubarMenu` renders a **controlled `DropdownMenu`** (`open` driven by the bar's `openValue`, `onOpenChange` → `setOpenValue`), and the menu-surface parts (`MenubarContent`/`Item`/`Label`/`Separator`/`Group`/`CheckboxItem`/`RadioGroup`/`RadioItem`/`Shortcut`) are DropdownMenu's components **re-exported under Menubar names** — they read `DropdownMenuContext`, which the wrapping `<DropdownMenu>` provides, so positioning (`computePosition`) / item arrow-nav / escape-restores-focus / outside-click all come free. Menubar's own new code is just the coordinator: (1) single-open state, (2) **hover-to-switch** (a trigger's `onMouseEnter` sets `openValue` to itself *only when a menu is already open*), (3) **roving tabindex + ArrowLeft/Right** across triggers (`moveFocus` reads a `stateRef` mirror so the keydown closure isn't stale; if a menu is open it opens the adjacent one). `MenubarTrigger` renders a `role="menuitem"` button wrapped in `DropdownMenuTrigger` (which composes our `onKeyDown` — ArrowLeft/Right — before its own ArrowDown/Enter/Space, and composes our ref so we can register the node). Trigger order comes from mount-order registration (`register(value)` in a `useEffect`). **Known v1 gaps:** no submenus (DropdownMenu has none — only ContextMenu does), and no ArrowLeft/Right *from inside* an open menu to jump to the adjacent one (only Up/Down/Escape inside; use hover or Escape-then-arrow). Verified light + dark. Only `.ui-menubar` + `.ui-menubar__trigger` are new SCSS; the menu surface reuses `.ui-dropdown-menu__*`.

- **InputOTP shipped (2026-07-23):** the segmented one-time-code input, off the deferred roster. **Built on the single-hidden-input model** (not N separate `<input maxLength=1>` fields): one real `<input>` owns the whole value and the `length` visual slots render underneath it — so native paste, mobile `autocomplete="one-time-code"` autofill and text selection all work for free, and `onChange` handles everything (paste is just a multi-char change → filter + `slice(0, length)`), no per-field focus juggling. The input is `position:absolute; inset:0; color/caret-color:transparent` overlaying the slots (z-index 1 so clicks focus it). Active slot = `input.selectionStart` (synced on focus/click/keyup/select); a blinking caret shows in the active *empty* slot. `pattern: RegExp` (default `/[0-9]/`, non-global so `.test` is stateless) filters allowed chars; `groupSize` inserts a `.ui-otp__separator` dash; `onComplete` fires once when length is reached; `label` composes the shared `<Label>`; `error`+`errorMessage`+`aria-describedby`; sizes sm/default/lg. Slot dimensions use `--w-*`/`--h-*` tokens (verified present before use — an undefined CSS var silently collapses a slot). **Remaining deferred roster:** Navigation Menu, App Shell, and the Calendar → Date Picker → Carousel → Resizable chain.
- **Direction (RTL/LTR provider) shipped (2026-07-23):** the easiest of the remaining deferred roster items. `DirectionProvider` sets the `dir` attribute on a **`display: contents`** wrapper (layout-neutral — no extra box) and shares `'ltr' | 'rtl'` via context; **`useDirection()`** reads it (for components that position in JS, e.g. `computePosition` surfaces — wiring that in is a future follow-up). No component changes were needed for the win: the library already leans on CSS logical properties (`margin-inline`, `border-inline-start`, `inset-inline`) and flex rows, so wrapping any subtree in `dir="rtl"` mirrors Button/Input/Chip/etc. for free (verified: icons flip sides, chips reverse order, text right-aligns). Naming mirrors `SidebarProvider`/`useSidebar`. `Direction.scss` is a single rule (`display: contents`). **Remaining deferred roster:** Navigation Menu, App Shell, and the Calendar → Date Picker → Carousel → Resizable chain.

- **Chat family — batch 4: editing, versions, reasoning, citations, greeting (2026-07-23):** the "buildable under the no-dep rule" enrichment layer (the remaining gaps vs. a production assistant were markdown/highlighting/STT/artifacts — all dep-blocked and still out). All **presentational** (consumer owns state/transport). **`ChatMessageEdit`** — inline editor reusing `.ui-input-wrap` + `useAutosizeTextarea`; Enter saves / Shift+Enter newline / Esc cancels; footer Cancel(ghost)+Save(aiden) `Button`s; consumer toggles the edit boolean and swaps it in for the bubble. **`ChatMessageVersions`** — a `‹ index/count ›` pager (prev/next disabled at the ends); consumer owns the versions array; regenerate is just a button in `ChatMessageActions` (no new part). **`ChatReasoning`** — a collapsible "thinking" block; **composes `Collapsible`** like `ChatToolCall` (Brain icon, chevron rotates on `[data-state=open]`, body indented with a left border); `thinking` shimmers the label (opacity pulse on `--duration-shimmer`). **`ChatCitation`** — inline marker (`<a>` when `href`, else `<span>`; small raised `--secondary` pill, hovers to `--primary` when a link). **`ChatSources`/`ChatSource`** — a labeled group of link-preview cards (index badge + title + domain + optional icon; `<a>` when `href`). **`ChatGreeting`** — dedicated centered start screen (icon/title/description + a slot for `ChatSuggestions`); owner chose a dedicated component over reusing `Empty`. Owner picked **editing + version pager** (the full interaction) and the **dedicated greeting**. Verified light + dark. **Now only the Figma/preview mirror of the whole family remains.**

- **Chat family — batch 3: composer extras + layout shell + streaming helper (2026-07-23):** the last of the deferred chat work (bar the Figma mirror). **`useStreamingText`** (new hook, see Shared utilities) — progressive type-out reveal, presentational/dep-free, pairs with the `streaming` caret. **`ChatComposerDictation`** — a mic toggle for the composer toolbar; **presentational only** (`recording` drives red + a `box-shadow` pulse ring on `--error-focus`; consumer wires real speech recognition); `aria-pressed`. **`ChatComposerDrawer`** — an expand button that opens a **`Drawer`** (default `side="bottom"`) holding a roomy textarea **bound to the same composer value via `useChatComposerContext`** (Enter is a newline here, not send — send is the drawer footer `Button variant="aiden"`, which submits + closes). **`ChatLayout` + `Header`/`Body`/`Footer`** — a **minimal** full-height flex shell (fixed header, scrollable body holding `ChatMessageList`, docked composer footer); **it provides the `density` context itself** (an alternative root to `Chat` for full-page apps — don't nest a `Chat` inside it). Dictation + expand share a `.ui-chat-composer__tool` icon-button base. Owner picked *minimal* shell + *light* streaming helper (not a full streaming manager). Verified light + dark: type-out reveal, dictation toggle (red+pulse), drawer open/value-sync/send-and-close, layout scroll + docked footer. **Only the Figma/preview mirror of the family remains deferred.**

- **Chat family — Claude-style restyle + batch 2 AI parts (2026-07-23):** two follow-ups to the MVP below. **(1) Restyle:** the first cut looked like an iMessage thread (two-sided tailed bubbles). Reshaped to the Claude conversation UI — **assistant turns are full-width, bubble-less prose** (transparent, no border/tail); **user turns keep a contained bubble on the trailing edge with uniform corners** (tail dropped); centered `max-w-3xl` column, larger airier text (`text-base`/`leading-7`), no per-message avatars (the `avatar` slot stays on `ChatMessage` for consumers). If you touch bubble styling, keep this asymmetry — don't reintroduce a bubble around the assistant. **(2) Batch-2 "core AI parts":** `ChatBubble` gained a **`streaming`** prop (blinking caret appended after content, distinct from `pending`'s pre-content dots; caret = `--w-0-5` × `1em` `currentColor`, `steps(1)` blink on `--duration-shimmer`). **`ChatToolCall`/`ChatToolCalls`** — collapsible agent-action cards: **composes `Collapsible`** (so it reuses the grid-rows animation + `id`-seeded ARIA; body padding sits on `.ui-chat-tool__body` *inside* the clip so it collapses to 0), `status` running/success/error → `Spinner`/`Check`/`CircleAlert` with `--muted-foreground`/`--success`/`--error`, name in mono, chevron rotates on `[data-state='open']`. **`ChatSuggestion`/`ChatSuggestions`** — one-shot prompt pills; **deliberately NOT `Chip`** (Chip is `aria-pressed` toggle semantics; suggestions are momentary buttons) — a dedicated outline-pill button. Tokens-only, verified light + dark. **Still deferred (batch 3):** `ChatComposerDrawer` (via `Drawer`), dictation mic button, page-level `ChatLayout`, real token-streaming plumbing, and the Figma/preview mirror.

- **Aiden chat family shipped — Conversation MVP (2026-07-23):** built the `Chat` compound family (`src/components/Chat/`) — the roadmap's deferred "chat family (bubble / message / message-scroller / marker)". **Code-first** (source of truth; Figma/preview mirror is the follow-up), **one family** with `sender`/`density` flowing via context (like Astryx / `Sidebar`). Scope was explicitly the **Conversation MVP**: `Chat` (root, `density` context) · `ChatMessageList` (auto-stick scroller + jump button) · `ChatMessage` (`from` context + alignment) · `ChatBubble` (neutral `--chat-bubble-*` tokens + tail + `pending` typing dots) · `ChatMessageActions` (hover/focus-within reveal) · `ChatMarker` (divider/system/status) · `ChatComposer`/`ChatComposerInput`/`ChatComposerActions`/`ChatComposerSend`. **Reuse, not reinvention:** composer box reuses `.ui-input-wrap` (so `Chat.tsx` **imports `../Input/Input.scss`** like InputGroup — without it the box has no border and the textarea keeps its native one), send composes `Button variant="aiden"` (gradient, on-brand under any theme; idle=`type=submit` so the form fires submit with **no** onClick or it double-fires; streaming=`type=button`+`onStop`), markers compose `StatusDot`, attachments reuse `Attachment`/`AttachmentGroup`, code uses `Code`/`CodeBlock`. **Two new SSR-safe hooks** (see Shared utilities): `useAutosizeTextarea` + `useStickToBottom`. **Owner decisions:** content is **consumer-provided** (children/JSX) — **no Markdown parser** (would need a dep); streaming is presentational (`pending` + `isStreaming`/`onStop` only). Transcript-part contexts are **non-throwing** (default `density: balanced`, `from: assistant`) so parts compose loosely / standalone in stories — unlike `Attachment`'s throw-guard; the composer sub-parts **do** throw (they're meaningless without the composer state). Tokens-only, verified in Storybook light + dark (Enter sends / Shift+Enter newline, autosize cap, auto-stick + jump, send↔stop, pending dots). **Deferred to a 2nd batch:** ToolCalls, suggestion chips, real token-streaming plumbing, `ChatComposerDrawer` (via `Drawer`), dictation, a page-level `ChatLayout`, and the Figma/preview mirror.

- **Category palette got an on-tint `-text` family + Badge `category` prop (2026-07-22):** the 17 `--category-*` hues were **never in the contrast audit**, and all 17 **failed WCAG AA as text on their own `-bg` tint in light mode** (1.79–3.95; 9 failed even the 3:1 graphical floor). Dark was already fine. Fix mirrors `--primary-text`: added **`--category-{c}-text`** (17×2) — light darkened via HSL just past AA on the `-bg` tint (4.56–4.83, "as light as it can be while passing"), dark reuses the base (already 4.77+). **Base `--category-{c}` stays vivid for fills/dots/charts; `-text` is for any small text on the tint.** Added all 17 `-text`-on-`-bg` pairings to `scripts/contrast-check.mjs` (now 40 pairings, 0 below AA). **Badge** gained a **`category?: CategoryColor` prop** (new shared type in `types/GlobalTypes.ts`, 15-hue union — yellow + lime later removed) plus **`categoryStyle?: 'soft' | 'solid'`** (default `soft`). `category` **overrides `variant`**. `soft` → `.ui-badge--cat-{c}` (`-bg` tint + `-text`); `solid` → `.ui-badge--cat-{c}-solid` (`--category-{c}` fill + `-foreground`). Both generated by a **SCSS `@each`** over `$ui-badge-categories` (**names quoted** — bare `cyan` is a Sass colour keyword that serialises to `aqua` and would emit the wrong class). `solid` uses the **vivid `--category-{c}` fill + inverted `--category-foreground`** (white in light / `#0f172a` in dark, exactly like `--error-foreground`). This one went through several rounds — white-on-vivid (fails AA on the lighter hues), darkened-for-white fills (muddy), a uniform-luminance `-solid` set (~2.6 contrast, pastel — owner rejected "pale easter egg colors"), and dark-ink-on-vivid — and the **owner's final call is white, accepting the AA dips** ("I don't care that they don't pass AA"; amber/green ~2.1 in light). Solid is best-effort, **not gated**; soft `-text` is the AA-safe path. **No `-solid` token** — the badge reads the vivid base directly. Mirrored to Figma as `category/{c}-text` + `category/foreground` (inverted). `CategoryColor` is the reusable vocabulary for extending category support to Chip/Item next.

- **Design-system audit → recorded "won't-fix" decisions (2026-07-22):** full audit (token coverage / completeness / naming) scored **90/100**. Token coverage is **perfect** (0 hardcoded hex/rgba/durations in component `.scss`; every `px` is a documented exception). Completeness near-total (all 54 dirs: `forwardRef`+`displayName`, exported, storied). **Only fix taken:** split `SpinnerProps` into `Spinner.types.ts` (was inline). The rest are **accepted — do NOT "fix" them, they've been evaluated:**
  - **BEM `__part` vs `-part` mixing is accepted.** Several components mix the element form (`.ui-x__part`) with a hyphenated pseudo-block (`.ui-x-wrap`, `.ui-dialog-overlay`, `.ui-field-label`). Both are valid; **do not mass-rename** — BEM was renamed once already and preview HTML + the Figma mapping depend on the current strings. Regression risk ≫ cosmetic gain.
  - **`ItemContent` (not `ItemBody`) is accepted** even though Item also has `ItemHeader`/`ItemFooter`. Renaming the export is a breaking change for consumers — not worth it for naming purity.
  - **Menu-item styling duplicated 4× (DropdownMenu, ContextMenu, Command, Select) stays** until a *5th* menu is needed — the existing policy. Don't extract a shared base pre-emptively.
  - **Dual `onChange` (event) + `onValueChange` (string) on Input/Textarea/NativeSelect is intentional** (adapter pattern) — both fire; just know both exist.
  - Modifier-prefix drift (`--sz-`, `--state-error`, `--default-default`) is accepted as-is.

- **Aiden is a `data-surface`, not a theme — the third axis (2026-07-21):** Aiden (the AI assistant) has its own UI *and* embeds inside every product brand, and its identity is a **violet→blue gradient**. That rules out making it a `data-theme`: (1) a gradient can't be the single scalar `--primary` (which must also work as border/text), and (2) a theme *swaps* a subtree's accent, but Aiden must *keep* its look inside another brand. So it's a **surface** — `data-surface='aiden'` — orthogonal to mode and theme, layering on top of any brand. **How the scope works (reuses ALL the theme machinery):** `[data-surface='aiden'] { --primary: var(--aiden-outline-border); --primary-foreground: var(--aiden-primary-foreground); }` (mirrors a theme scope). That one remap turns every *scalar* `--primary` consumer violet for free — outline/link/secondary text (via derived `--primary-text`), borders, focus rings, checked checkbox/switch/radio, active chip. Then **every `--primary` FILL** takes the actual gradient via a `[data-surface='aiden'] …{ background: var(--aiden-primary) }` override in its **own component SCSS** — **10 components**: Button primary CTA, Badge default, Chip active, Checkbox checked, Switch track, **RadioGroup checked dot, Progress determinate default bar, Slider range, InputGroup default button, and Fab** (the floating "Ask Aiden" launcher — the gradient is its whole point; rings stay solid `--primary`). (Rule: *anywhere `--primary` is a background fill, use the gradient*; where it's text/border, the solid violet via `--primary-text` / `--primary-border` stays.) The Progress override is scoped `:not(.ui-progress--indeterminate)` so the shimmer keeps animating, and to `--default` so semantic variants keep their colour. **Ghost stays neutral slate** — same carve-out as the brand themes. Nests correctly inside any `data-theme` (proven: a db button indigo, an Aiden panel beside it violet). Key gotchas: (a) **the Chip's active-hover override must match the base hover specificity** — base `.ui-chip--active:hover:not(:disabled)` is (0,3,0) and beats `[data-surface='aiden'] .ui-chip--active` (0,2,0), so add `[data-surface='aiden'] .ui-chip--active:hover:not(:disabled)`; same reasoning applied to the Button CTA hover. (b) **axe can't measure gradient contrast** — verify white-text-on-gradient by painting the gradient to a canvas and sampling across the label width, not with axe.
- **Aiden identity moved to "blurple" `#5a37e6` (2026-07-28) — supersedes the 2026-07-21 tuning below:** the solid Aiden accent was the old violet `#8455f0` (hue 258°), which sat only ΔE 13.9 from `db`'s indigo primary and cleared white-text AA by a hair (4.63). Owner wanted more gravitas without colliding with `db`. **The trap:** `db-primary` is *itself* an indigo-violet (`#6063f1`, hue 239°) and the old gradient's mid stop was ΔE **0.8** from it — so leaning *toward* indigo for gravitas runs straight into `db`. **Fix = "blurple":** solid `--aiden-outline-border` → `#5a37e6` (hue 252°, *between* violet and db-indigo). It stays distinct from `db` by sitting **darker** (L56 vs db's L66), not by hue — ΔE 24.8, white-text 6.7:1. Dark solid → `#9076f9` (a saturated-but-light blurple, dark-text 5.22:1 — a *deliberate* dark shift, not the near-invisible 6° nudge first tried). **Gradient `--aiden-primary`** kept airy on purpose: opens bright on the base violet `#8455f0`, lands on the blurple core `#5a37e6`, resolves to blue `#2c6dea` (blue end unchanged, so the tightest AA point holds at 4.63). Deep blurple is reserved for the *solid* (text/border/ring/halo) where depth helps contrast; the *fill* stays energetic. All derived tints re-anchored. **Aiden secondary-button hover (settled 2026-07-28):** it must behave like *every other* secondary button — the shared mode-aware `--opacity-50` overlay (`rgba(255,255,255,.5)` light / `rgba(15,23,42,.5)` dark) over `--aiden-secondary`, so it **lightens in light and DARKENS in dark**. First attempts got this wrong (a bright near-opaque flash, then a lighter tint-deepen); the bespoke `--aiden-outline-hover` token was removed entirely. **Also:** `data-surface='aiden'` now overrides `.ui-button--default-secondary` (fill + text/border + this hover) so the surface secondary renders identically to `variant="aiden"` secondary — previously the surface only overrode the primary CTA, so secondary diverged. **Contrast: aiden solid-*fill* hover (`--aiden-hover`) still goes *lighter* in dark** (solid fills carry dark text, so lighter = more contrast); the *secondary tint* button darkens (light blurple text wants a darker backdrop). Different button types, different correct directions — both match the rest of the system. Note `db`/theme primaries already lighten on hover in dark too (audited 2026-07-28: `--primary-hover` = `color-mix(--primary 85%, --foreground)`, auto-adapts by mode — no bug there). Colours verified via `scripts/contrast-check.mjs` + canvas gradient sampling. Values below (2026-07-21) are the prior violet tuning, kept for history.
- **Aiden gradient re-tuned for AA (2026-07-21):** the original light gradient (`#8b5cf6→#7761f3→#60a5fa`) was too light for **white** text at *every* stop (4.23 / 4.39 / 2.54 — the blue end failed hard), so the flagship "Ask Aiden" button, default badge, and active chip all sat under AA (axe couldn't see it because it's a gradient). Fixed the **light** gradient to `#8455f0 → #5f61ef → #2c6dea` (violet→indigo→blue) — the **lightest** stops where white text still clears AA (canvas-measured min **~4.6** across a long chip label; owner explicitly wanted it "as light as it can be, right on the line"). Hover deepens to `#7c3aed→#4f46e5→#2563eb` (min 5.34). **Dark mode is unchanged** (`#a78bfa→#93c5fd` carrying **dark** text — already passes). Owner **rejected** the dark-text-on-light-pastel-gradient option for light mode ("way harder to read"). The `--aiden-secondary`/`-border`/`-ring`/`-focus` tints + `--aiden-outline-border` (now `#8455f0`) were re-derived to match. POC lives at `src/prototypes/AidenSurface.stories.tsx`; the mechanism is documented in `Foundations/Themes → Aiden Surface`. Follow-up if wanted: extend the surface to more components (the standalone `variant="aiden"` on Button/Badge still exists for one-off accents).
- **Accessibility audit of composed pages → `--primary-text` + 3 component fixes (2026-07-21):** ran axe-core 4.10.2 over three composed prototype pages (`src/prototypes/`) across **both modes × all 9 themes**. Everything passed except a few real issues, all now fixed:
  - **`--primary-text` on-surface token** (see Token vocabulary + the superseded secondary-button note) — fixes themed `--primary`-as-text falling just under AA (outline buttons ~4.33 in db dark; brand Banner/Alert titles 4.22–4.28 in light for 6 themes). Repointed Button (secondary/outline/link), Badge (outline), Alert (brand), Banner (brand). **All 18 mode×theme combos now 0 contrast violations** on the dashboard.
  - **Progress had no accessible name** (`role="progressbar"` with an unassociated label) — now wires `aria-labelledby` to the rendered label via `useId` (only on the simple, non-children path; children/no-label consumers pass their own `aria-label`).
  - **Alert semantic descriptions ran `opacity: var(--o-80)`**, dragging colored-on-tint text below AA (info 3.7). Removed the opacity from all 5 (brand/info/success/warning/error) — full opacity clears AA (info 5.37 … error 4.72); size+weight still carry the title/description hierarchy.
  - **Tabs inactive-trigger colour froze on a live mode-switch** — `.ui-tabs__trigger` transitioned `color: var(--muted-foreground)`, and **Chromium freezes a `var()`-driven transitioned property when the variable changes without a recalc** (→ 1.36:1 in dark). Dropped `color` from the trigger transition. **General lesson:** this freeze hits *any* transitioned var-colour on a dynamic `data-mode` flip (breadcrumb links, active-tab background, etc.); it's invisible on fresh load and the ModeToggler's view-transition path recalcs it away, but the **reduced-motion path (plain commit) does not** — so verifying dark-mode contrast by toggling `data-mode` needs a forced recalc (`el.style.display='none'; el.offsetHeight; el.style.display=''`) or the results are polluted by freeze artifacts.
  - Remaining page findings are **prototype-markup only** (no `<main>` landmark, `h1→h3` skips, a couple `opacity:0.7` decorative texts) — not component defects.
- **`--border-hover` token + Card `interactive` prop (2026-07-21):** added `--border-hover` (light `#64748b` / dark `#cbd5e1` — see "Radii / borders") for a border that strengthens on hover. Three consumers wired: (1) **Card gained an opt-in `interactive` prop** (`.ui-card--interactive`, matching Item's existing `interactive` convention) — on hover it strengthens the border to `--border-hover`, **lifts `translateY(-4px)`** (`--motion-slide-sm`), and deepens the shadow `--shadow-xs → --shadow-md`, all over `--duration-normal`/`--ease-out`; off by default so static cards are untouched. The lift has a **local `@media (prefers-reduced-motion: reduce)` override setting `transform: none`** — the global reduced-motion block only zeroes *durations*, so without this the card would still teleport up 4px; border + shadow stay (not motion). (2) **Item `--outline`** variant's interactive hover: border `--border` → `--border-hover` (default/muted variants keep their background-`--accent` hover — a border appearing from `transparent` would clash). (3) **`.ui-input-wrap`** hover: was `--muted-foreground`, now `--border-hover` (covers Input/Textarea/NativeSelect). Borders are non-text, so `--border-hover` is not in the contrast test's pairings.
- **Muted-text surfaces moved to `--secondary`; contrast guardrail added (2026-07-21):** `--muted` in light was slate-300 (`#cbd5e1`) — the *darkest* neutral surface, literally the same value as `--border`. That made muted-filled panels read too heavy AND put `--muted-foreground` text on them at only **5.1:1** (the tightest pairing in the system, sharpest on Kbd at 12px). Fix: repoint the **five surfaces that carry `--muted-foreground` text** — Alert default, Banner default, Item `--muted` variant, Avatar fallback initials, Kbd — from `--muted` → `--secondary` (slate-200). **Text hierarchy is unchanged** (title `--foreground` / description `--muted-foreground`, per the owner — "the point of muted description text is hierarchy"); only the *surface* lightens, lifting the pairing to **6.15:1 light / 11.87:1 dark**. **Pure image/icon fills stay on `--muted`** (Item/Attachment image tiles, Empty icon tile, Avatar count badge) — they carry no small muted text and want the weight. **Rule going forward: `--muted-foreground` text belongs only on `--background`/`--card`/`--secondary`; never introduce a new `--muted-foreground`-on-`--muted` pairing.** New guardrail **`npm run test:contrast`** (`scripts/contrast-check.mjs`, dependency-free) parses `tokens.scss` — brace-matching each `[data-mode]` block so the bottom-of-file `[data-theme]` scopes don't pollute the dark `--primary` values — composites `-light` tints over their base surface, and **fails below AA (4.5:1)** across 23 text-on-surface pairings. Add a pairing there whenever a component puts a `-foreground`/text token on a new surface. The owner explicitly rejected a `--tertiary`/`--subtle` surface token for this — the `--secondary` repoint solved it without new API.
- **`inherit` in component SCSS is wiring, not a gap (2026-07-21):** an audit (prompted by "define everything we can") found 36 `inherit` usages. Unlike an untokenized hex/px, `inherit` is a **defined relationship** and is mostly load-bearing: `color: inherit` on Alert/Banner/Attachment/Breadcrumb close-buttons/links is *how* a variant's `-foreground` colour reaches the nested element (pinning a token freezes it to one colour and breaks variant theming); `border-radius: inherit` (Progress indicator, Attachment overlay) keeps inner/outer radii in lockstep; `font-size`/`line-height`/`color: inherit` on native form controls follow the `--sz-*` size modifier and disabled/error colour set on the wrap; `font-family: inherit` is the idiomatic native-control reset (kept over `var(--font-family)` deliberately — pinning is more explicit but less flexible for consumers who theme a section's font). **Only 2 were pinnable and were pinned:** Item/Attachment `__title` `color: inherit` → `var(--foreground)` (they always resolved to foreground via cascade). Don't "tokenize" the rest — it's a regression.
- **Component batch — 10 new components + ripple (2026-07-20):** shipped in 7 committed groups off the "finish the roster" push. AspectRatio, Kbd, Blockquote, StatusDot (trivial primitives); Toggle + ToggleGroup (segmented); Banner (page-level bar); AlertDialog; Pagination; Code + CodeBlock; plus the `useRipple` hook. Deferred as their own efforts (too large for one session under the no-dep rule): Calendar, Date Picker, Carousel, Resizable, Menubar, Navigation Menu, Input OTP, Direction, App Shell, and the **chat family** (bubble / message / message-scroller / **marker** — "marker" is the inline status/system-note/labeled-separator chat part, per the owner). Overlaps left as reuse, not rebuilt: **Sheet → Drawer**, **Power Search → Command**, Attachment already shipped. Key sub-decisions:
  - **AlertDialog is a preset of Dialog, not a reimplementation.** The only Dialog change is a new optional `role?: 'dialog' | 'alertdialog'` (default `dialog`, backward-compatible). AlertDialog passes `role="alertdialog"`, `closeOnOutsideClick={false}`, and header `showCloseButton={false}` — the two carve-outs vs a plain dialog. It has **no `.scss` of its own** (reuses Dialog.scss).
  - **Toggle is distinct from Chip** (owner's call): Chip = rounded filter pill, Toggle = squared `aria-pressed` control with a neutral `--accent` pressed fill. Toggle reuses Chip's interaction shape; ToggleGroupItem reuses `.ui-toggle`.
  - **Pagination cells are `<a class="ui-button">`** — so Pagination.tsx must `import '../Button/Button.scss'` (cells render with no `<Button>` nearby, so its CSS wouldn't otherwise load in isolation), and `.ui-pagination__link` sets `text-decoration: none` (Button was only ever used on `<button>`, which has no underline). Same class-reuse pattern as NativeSelect→Input, ToggleGroup→Toggle.
  - **Ripple is a hook, not a Button prop (2026-07-20).** First shipped as an opt-in `ripple` prop on Button; the owner then **removed the prop** so it can't be over-used — a click that navigates away never shows the wave, and it's a Material signature much of modern web design omits. `useRipple` stays as an exported hook for the narrow cases it fits (touch-first in-place actions); when/how is documented in the hook JSDoc + `Hooks/useRipple` stories. Duration is `--duration-ripple` (600ms, tuned to animate-ui's reference — 300ms read as too quick); the 900ms `setTimeout` fallback is load-bearing since `animationend` doesn't fire under reduced motion.
- **Theming re-architecture (2026-07):** `--primary` = "the current theme's color." Mode (light/dark) = `data-mode`; theme (sub-brand) = `data-theme='{code}'` which remaps `--primary`. Main brand = no `data-theme`. The old `--brand` family and `variant="brand"` were **removed** — don't reintroduce them. Full immersion except Tooltip (uses `--tooltip-*`). See `Foundations/Themes`.
- **Button emphasis ladder (2026-07):** every variant (default + error/info/success/warning + aiden) has five visibly distinct rungs — `default` (solid fill) → `secondary` (filled `-soft` tint + border + colored text) → `outline` (transparent + border + colored text) → `ghost` → `link`. `secondary` must use `-soft` (not `-light`) or it collapses into `outline`. `default`'s `ghost` is the neutral-slate carve-out; aiden keeps its gradient on `default`/`secondary` fills but uses solid violet (`--aiden-outline-border`) for text + outline/ghost/link.
- **Secondary-button AA — FIXED via `--primary-text` (2026-07-21).** Previously (2026-07) the themed `default`-secondary text sat at 3.84–4.34 in light and the owner left it as-is. The `--primary-text` on-surface token (see Token vocabulary) is exactly the "deeper on-fill text (Material tonal pattern)" fix that note anticipated: secondary/outline/link button text, Badge `outline`, and brand Alert/Banner titles now read `--primary-text`, clearing AA across all 9 theme codes × both modes (verified below). Don't reintroduce raw `--primary` as text on those. **Not covered: the `aiden` gradient variant** — its outline/secondary text is `--aiden-outline-border` (solid violet), still ~3.84 in light. Same class of issue; would need an analogous `--aiden-text` on-surface token if you want it fixed too (not done — no page exercised it).
- **Reduced motion is handled globally in `tokens.scss` (2026-07-19):** an accessibility audit found **27 of 42 components animate and only ModeToggler checked `prefers-reduced-motion`** — no global rule existed. `tokens.scss` now ends with a `@media (prefers-reduced-motion: reduce)` block collapsing all animation/transition durations. **It uses `0.01ms`, not `animation: none` — this is load-bearing.** Drawer and Tooltip unmount off `animationend`; `none` means that event never fires and the panel stays mounted forever, which is exactly the stuck-open trap the Drawer notes above warn about. A near-zero duration still fires the event on the next frame. Verified: with the rules applied unconditionally, Drawer still goes `open → closing → unmounted`. **Spinner is the deliberate exemption** (re-declared at `--duration-spin`, `infinite`) — it's a functional status indicator and freezing it reads as "nothing is happening". If you add a component with its own reduced-motion rule, prefer the global block over a local one.
- **Chip got the system focus ring (2026-07-19):** `Chip.scss` had no `focus`, `outline` or `box-shadow` rule at all, so it fell back to the browser's default outline rather than the 3px token ring. Now matches Checkbox / Switch: `outline: none` + `box-shadow: 0 0 0 var(--focus-ring-width) var(--focus)`. Same class of gap as the Toast one below — worth grepping a new component's SCSS for `focus-visible` before shipping it.
- **Button gained an `xsmall` size, and Toast composes `Button` (2026-07-18):** Toast's action/cancel were hand-rolled `<button>`s in `Toast.scss` — 2px radius instead of Button's `--rounded-md`, a wrong hover (`--foreground` rather than `--primary-hover`), no disabled state, no participation in the variant/style ladder, and **no design-system focus styling** (Button has 27 focus rules; these had none). **Correction (2026-07-19):** the original note here — and commit `00615ea`'s message — said keyboard users got *no visible focus*. That overstated it. The old rules contained zero `outline` declarations, so they never suppressed the browser's default ring; keyboard users saw that instead of the system's 3px `--focus-ring-width` ring. Inconsistent, not invisible. The fix was still right, but don't cite this as an example of a missing focus indicator. Fixed by adding a fourth Button size **`xsmall`** (`--p-1`/`--p-2-5`, `--text-xs`, 24px tall — a tighter box on the same type ramp as `small`) and rendering real `<Button>`s in `ToastCard`: action = `style="default"`, cancel = `style="outline"`, both `size="xsmall"`. `.ui-toast__action` / `.ui-toast__cancel` survive as **layout-only hooks** so consumers can still target the slots. The shared `Size` type in `types/GlobalTypes.ts` is consumed by **Button only**, so widening it rippled nowhere else. Net effect: focus rings, disabled, and full theming now come free — verified `data-theme='dr'` turns both buttons crimson, which the old neutral `--muted-foreground` cancel never did. **Don't re-inline button styling into a surface's SCSS** — compose `Button` at `xsmall`.
- **State updaters must stay pure — Toaster (2026-07-18):** `Toaster` used to call `onDismiss()`, `clearTimer()` and `scheduleDismiss()` *inside* `setToasts((prev) => …)`. React treats updaters as pure and may run them more than once — StrictMode does in dev — so every consumer `onDismiss` fired **twice** (measured: 2 calls for 1 dismissal). Fixed by funnelling writes through a `commit()` helper that keeps a `toastsRef` mirror in lockstep, so handlers read the current stack outside any updater and side effects run exactly once. The ref assignment is synchronous, which preserves the same-tick-burst correctness the old `prev` argument provided. **Don't move side effects back inside an updater** in Toaster or anywhere else.
- **`Toaster` is an accepted exception to the `forwardRef` + `id` rules (2026-07-18):** hard rules 4 and 6 say every component gets `forwardRef` and a required `id`. `Toaster` deliberately has neither. It's a mount-once portal singleton with no meaningful ref target for callers and no child IDs to seed — adding them would be API surface that serves the rule rather than any consumer. Owner decision, not drift. Every *other* component still follows rules 4 and 6.
- **Toast duration precedence (2026-07-18):** `<Toaster duration>` used to be a **no-op** — `toast.ts` baked `DEFAULT_DURATION = 4000` into every record at creation, so the Toaster never had anything to fall back to. Now non-error toasts leave `ToastRecord.duration` **undefined** and the Toaster substitutes its own prop. Resolution order: **per-toast `opts.duration` → variant default (error only, 6000ms) → `<Toaster duration>` → 4000**. `toast.loading()` / the pending leg of `toast.promise()` still pass `Infinity` explicitly and stay sticky; `promise`'s success leg patches `duration: undefined` so it falls back to the Toaster rather than re-hardcoding 4000. Don't reintroduce a non-error default inside `toast.ts` — that's exactly what broke the prop.
- **Accordion root strips its own props from `...rest` (2026-07-18):** `value` / `defaultValue` / `onValueChange` / `collapsible` were leaking onto the root `<div>` (React warned on `collapsible`; the string ones rendered as real DOM attributes). They can't simply be destructured off `props` — `type` has to stay an *aliased discriminant* of `props` or `type === 'single'` stops narrowing `props.value`/`props.defaultValue` and the file won't typecheck. Hence the deliberate two-step destructure against `AccordionRootValueProps`. Don't "simplify" it back into one.
- **Motion tokens exist now** — use `--ease-spring` / `--ease-spring-strong` and `--motion-slide-sm/md/lg` instead of raw literals.
- **Storybook is pinned to 8.6.18** (don't upgrade to 9/10 casually — addon-essentials was removed in 9+).
- **Sass modern-compiler API** is enabled in `.storybook/main.ts` viteFinal — keeps the terminal quiet.
- **Light-theme semantic hues darkened for WCAG AA (2026-07):** `--error #d32424`, `--success #047857`, `--warning #c2410c` (plus matching `-light/-border/-ring/-focus/-hover`). All now ≥4.5:1 as text on white AND on their own `-light` tints. Dark theme untouched. Don't lighten these back without re-running contrast math.
- **Surface rule:** floating overlays (Popover, HoverCard, DropdownMenu, ContextMenu, Select, Combobox, Command, Toast) use `--popover`; in-flow surfaces (Card, Dialog panel) use `--card`. Tooltip is the deliberate exception (inverted `--primary`).
- **Composition rule:** dismissal X = `CloseButton` (Dialog, Alert, Toast all comply); form-label typography = shared `.ui-label__*` classes (Field included); hairline-with-label = `Separator` (FieldSeparator wraps it); **in-surface action buttons = `Button`** (Toast's action/cancel comply — see below); **small icon-only buttons = the shared `.ui-icon-button` shell** (see below).
- **`.ui-icon-button` — the shared icon-button shell (2026-07-26).** `src/styles/icon-button.scss` owns the box, hover tint, focus ring, disabled state and WCAG 2.5.8 hit-target expansion for every small icon-only button. Extracted at the third copy: CloseButton, AttachmentAction and the Chat composer tools had each rebuilt it and drifted — only CloseButton carried the hit-target work, and the transition timings disagreed. Consumers add the class in their `className` and own only their box size, icon size, `--ui-icon-button-radius` (default `--rounded-md`), `--ui-icon-button-hit` (a NEGATIVE inset, required on any box under 24px) and `--fill` if hover should tint the background rather than just the glyph. Imported as a side effect the way `ripple.scss` is. **`CloseButton` hard-codes an `X` and is only ever a dismissal** — anything else needing a small icon button takes the shell directly rather than wrapping CloseButton or rebuilding it.
- **Badge model (2026-07):** flat variant list, `{solid base, transparent outline}` per color. `secondary` and `no-background` were removed. Every `outline` — neutral and semantic — is now **transparent + colored border + colored text** (no fill), so `-outline` means the same thing across all colors. `default`/`outline` theme via `--primary`; semantic colors keep their own. Badge extends `React.HTMLAttributes<HTMLDivElement>` and spreads `...rest` like every other component.
- **Known-intentional duplication (future consolidation candidates):** menu-item styling exists 4× (DropdownMenu, ContextMenu, Command, Select); Attachment/Item are parallel compound families. Don't add a 5th copy — extract a shared base if you need another menu. (**Menubar did the right thing here — it reuses DropdownMenu wholesale rather than copying**, so the count stays 4.)
