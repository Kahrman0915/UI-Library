export const spinnerSize = {
  xsmall: 12,
  small: 16,
  default: 16,
  large: 20,
} as const;

export const aidenStyles = new Set<string>([
  'default',
  'secondary',
  'outline',
  'ghost',
  'link',
]);
